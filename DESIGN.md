# KT ENA 편성 AI Agent — 설계 문서 (DESIGN)

> PLAN.md의 작업 순서를 실제로 어떻게 만들지 구체화한 문서. bkit PDCA Design 단계를 참고했으며, 비개발자도 이해할 수 있도록 화면 구성·데이터 흐름·기술 선택 위주로 쉽게 작성했다.

| | |
|---|---|
| **기준 문서** | [PLAN.md](PLAN.md), [PRD.md](PRD.md) |
| **상태** | 초안 |

---

## 1. 화면 구성 (무엇이 어디에 보이는지)

서비스는 크게 **① 관리자 화면(로그인 필요)**과 **② PD 화면(공유 링크로 접속)** 두 종류로 나뉜다.

### 1.0 디자인 톤앤매너 (전체 원칙)

참고 이미지(헬스케어 대시보드)의 스타일을 우리 서비스 언어로 옮겼다.

| 참고 이미지 요소 | 우리 서비스 적용 |
|---|---|
| 부드러운 파스텔 블루·라벤더 그라디언트 배경 + 흐릿한 블롭(blob) 장식 | 동일하게 적용 — 배경은 은은한 그라디언트, 카드는 그 위에 떠 있는 느낌 |
| 화이트 라운드 카드 + 은은한 그림자(글래스모피즘) | 모든 카드(채널 카드, 지표 카드, 인사이트 카드)에 동일 적용 — 모서리를 크게 둥글리고(16~24px), 그림자는 약하게 |
| 좌측 얇은 아이콘 사이드바 | Page 2의 "채널 선택" 목록에 적용 (로고 아이콘을 세로로 배치) |
| 상단 헤더: 인사말 + 아바타(좌) / 유틸리티 버튼 + 강조 CTA 버튼(우) | "좋은 아침입니다" + 오늘 날짜(좌) / 알림·설정 아이콘 + "새로고침/내보내기" 버튼(우) |
| 큰 히어로 카드(굵은 라벨 + 큰 타이틀 + 임팩트 있는 3D 비주얼 + 하단 입력창) | Page 1의 "① ENA 채널 하이라이트" 카드에 적용 — 큰 순위·시청률 숫자를 임팩트 있게, 하단에 자연어 질문 입력창은 Page 2로 이동(요청사항 유지) |
| 캘린더 히트맵(월간 진행률, 파스텔 색상 점) | Page 1 "④ 전체 채널 비교" 자리에, 일별 목표 달성 상태를 색상 점으로 표시하는 캘린더 히트맵으로 활용 |
| 4개 통계 카드(아이콘 + 라벨 + 강조 숫자 pill + 미니 차트) | Page 2의 "HOW DEEPLY?" 등 지표 섹션에 적용 — Rating/Share/Reach/Time Spent 각각 아이콘+숫자 pill+미니 차트 카드 |
| 하단 인사이트 카드(색상 태그: POSITIVE/TAKE ACTION/MONITOR) | "AI Insights" 카드에 적용 — 🟢 STRENGTHEN(긍정) / 🟡 WATCH(주시) / 🔴 RISK(위험) 태그로 치환 |
| 체크리스트형 카드(항목 + 완료 아이콘) | "편성 추천 액션" 카드에 적용 — KEEP/MOVE/REPLACE 등 추천을 체크리스트 형태로 |
| 인물 사진 + 일정 카드 | "다음 브리핑/자료 갱신 일정" 카드로 대체 (인물 사진 대신 채널 로고) |

**색상 적용 원칙**
- **Page 1(종합 대시보드)**: 특정 채널에 치우치지 않는 **중립 톤**(참고 이미지와 같은 블루·라벤더 파스텔 그라디언트)을 기본으로 사용한다. KT ENA 회사 CI 로고 전달 시, CI 컬러로 교체할 수 있다.
- **Page 2(채널별 딥다이브)**: 카드 배치·구성은 Page 1과 동일한 언어를 유지하되, **선택한 채널의 로고 색상**을 배경 그라디언트·강조 버튼·차트 색상에 반영해 채널마다 다른 톤을 준다.
  - 1차: `채널 로고/` 폴더의 로고 이미지에서 대표 색상(dominant color)을 자동 추출해 Channel Master에 저장
  - 2차: 정식 브랜드 컬러 가이드가 도착하면 관리자가 Channel Master에서 직접 값을 수정할 수 있게 한다
  - 예: ENA는 로고의 메인 톤, ENA Drama는 로고의 메인 톤, OLIFE는 로고의 메인 톤 — 각 채널 화면을 열면 그 채널 로고 색이 화면 전체 그라디언트·포인트 컬러로 은은하게 깔린다

### 1.1 전체 화면 구조

```
[공유 링크로 접속한 PD]              [로그인한 관리자 2명]
        │                                    │
        ▼                                    ▼
┌───────────────────┐              ┌───────────────────────┐
│  Page 1             │              │  Page 1 / Page 2       │
│  종합 대시보드        │◀── 공통 ──▶│  (PD와 동일 화면 +      │
│  Page 2              │              │   업로드 관리 메뉴)     │
│  채널별 딥다이브       │              │                        │
└───────────────────┘              │  + 업로드 관리 화면      │
                                    │    - Nielsen Excel     │
                                    │    - skyUHD 파일        │
                                    │    - 목표 시청률         │
                                    │    - Channel/Competitor │
                                    │      Master (로고·컬러)  │
                                    │    - 공유 링크 재발급    │
                                    └───────────────────────┘
```

### 1.2 Page 1 — 종합 대시보드 (첫 화면)

**설계 변경(2026-08-19, 사용자 지시)**: 참고 이미지(SYNAPSE 헬스케어 대시보드)의 파스텔 블루·
라벤더 그라디언트 + 블러 블롭 장식 + 글래스모피즘 화이트 카드 톤을 그대로 따르고, 레이아웃은
**2열×3행 그리드**로 재구성했다.

```
┌──────────────────────────────────────────────────────────┐
│  [ENA 로고] KT ENA YYYY-MM-DD 채널 현황      ⚙  [🔄 새로고침] │  ← 헤더(인사말 대신 로고+제목)
├───────────────────────────────────┬──────────────────────┤
│ R1C1 채널 현황                       │ R1C2 Original 성과     │
│  ENA 히어로(가장 크게) + 나머지 6채널  │ (표 형태: 본방/직후재방/ │
│  압축 리스트(시청률+등위)             │  동시간대 경쟁 프로그램)  │
├───────────────────────────────────┼──────────────────────┤
│ R2C1 채널별 인사이트(줄글)             │ R2C2 채널별 킬러 콘텐츠  │
│  ENA→ENA Play→ENA Drama→OLIFE→       │ (같은 순서, daypart     │
│  ONCE→ENA Story 순, 오늘 vs 최근      │  강세·약세 시간대 포함)  │
│  4주 평균 편차가 큰 것만 문장화        │                        │
├───────────────────────────────────┴──────────────────────┤
│ R3 오늘의 빠른 요약 (전체 폭)                                │
│  🟢🟡🔴 STRENGTHEN/WATCH/RISK 태그 (기존 규칙 기반 유지)      │
└────────────────────────────────────────────────────────┘
```

R2/R3의 정확한 셀 배치는 사용자가 명시하지 않아 임의로 정한 설계 판단이다(CLAUDE.md 참고).

### 1.3 Page 2 — 채널별 딥다이브 (선택한 채널 로고 색상 테마)

레이아웃 언어는 Page 1과 동일하되, 배경 그라디언트·포인트 컬러·차트 색이 **선택한 채널의 로고 색상**으로 바뀐다 (아래는 예: ENA Drama 선택 시).

```
┌───────┬──────────────────────────────────────────────────┐
│ 채널   │ [ENA Drama 로고] [기간: 오늘 ▾]                    │  ← 배경 그라디언트가
│ 로고   │ 시청률 0.132 (32위) ▲3   목표 대비 누적: OO위         │    ENA Drama 로고 색으로
│ 목록   │──────────────────────────────────────────────────│
│ (좌측  │ WHAT HAPPENED?   무슨 일이 있었나 (추이 그래프)        │
│ 사이드 │ WHY?             왜 그런가 (원인 후보)                │
│ 바,    │──────────────────────────────────────────────────│
│ 참고   │ ┌────────┬────────┬────────┬────────┐              │  ← 참고 이미지의
│ 이미지 │ │Rating  │Share   │Reach   │TimeSpent│              │    4개 통계 카드
│ 스타일)│ │숫자pill │숫자pill │숫자pill │숫자pill │              │    (아이콘+미니차트)
│        │ │미니차트 │미니차트 │미니차트 │미니차트 │              │    그대로 차용
│ ENA    │ └────────┴────────┴────────┴────────┘              │
│ ENA    │──────────────────────────────────────────────────│
│ Drama  │ CONTENT FITS? / OPPORTUNITY? / WHAT TO SCHEDULE?     │
│ ENA    │ (Fit Score 카드 — 참고 이미지 "Care Plan" 체크리스트   │
│ Play   │  스타일로 KEEP/MOVE/REPLACE/STRENGTHEN/TEST 표시)      │
│ ENA    │──────────────────────────────────────────────────│
│ Story  │ COMPARED WITH? 경쟁채널 순위표 + 등락·최고 성적 프로그램│
│ OLIFE  │  보고서 줄글 (2026-08-20 재설계, 막대그래프 대체)        │
│ ONCE   │──────────────────────────────────────────────────│
│ skyUHD │ 02시~26시 시간대별 막대그래프 (지표 체크박스 + 시간대별   │
│        │  실제 편성 프로그램명 tooltip·하단 목록)                │
│        │──────────────────────────────────────────────────│
│        │ 💬 자연어 질문 (채팅창, 참고 이미지 "Chat with SYNAPSE" │
│        │    입력창 스타일 — 채널 색 그라디언트 전송 버튼)         │
└───────┴──────────────────────────────────────────────────┘
```

**2026-08-20 재설계**: 위 8대 질문 섹션은 카드 나열이 아니라 대부분 **보고서 줄글 + 표** 형태로 다시 짜였다(사용자 지시) — 오늘의 브리핑은 What/Why 라벨 없는 하나의 문단, WHO IS WATCHING?/HOW DEEPLY?는 숫자 옆에 의미를 풀어쓴 문장, CONTENT FITS?는 채널 기여도 높은 순 표, OPPORTUNITY?는 daypart(새벽/오전/오후/저녁·심야)별 "우리 vs 경쟁채널" 격차가 최근 1주 사이 어떻게 좁혀졌는지 짚는 문단이다. 자세한 설계 판단은 DATA_DICTIONARY.md §9 참고.

**2026-08-20 기간 설정 확장 + 섹션 추가(같은 날 두 번째 사용자 지시)**: 우측 상단 "기간: 오늘 ▾"가 **단일 일자**(DoD/WoW/MoM/QoQ/YTD/직접 선택)와 **기간 범위**(최근 7일/1개월/3개월/1년/YTD/YYYY-MM-DD~YYYY-MM-DD 직접 선택) 두 모드를 토글로 고르는 형태로 바뀌었다 — 기간 범위를 고르면 오늘의 브리핑부터 COMPARED WITH?까지 대부분의 섹션이 그 기간 평균/등락으로 재계산된다(상세 함수별 설계 판단은 DATA_DICTIONARY.md §10). 화면 순서도 바뀌었다: 오늘의 브리핑 바로 아래로 02~26시 시간대별 그래프를 옮기고, 그 다음에 **최근 12주 요일×시간대 강세 히트맵**과 **시청률 상위 콘텐츠 TOP 20**(강세/약세 시간대 요약 포함) 두 섹션을 새로 추가했다 — 이 두 섹션은 skyUHD처럼 매일 갱신되지 않는 채널도 12주 누적으로 보면 패턴이 드러난다는 사용자 설명에 따라 기간 선택과 무관하게 항상 최근 12주 고정 윈도우로 계산한다.

**2026-08-20 DoD/WoW 버그 수정 + 프리셋 통합(같은 날 세 번째 사용자 지시)**: 위 "단일 일자" 모드의 DoD/WoW/MoM/QoQ가 기준일 자체를 과거로 옮겨버려, 오늘의 브리핑 등이 어제/전주를 마치 "오늘"인 것처럼 서술하는 문제가 있었다(사용자 지적) — WHAT HAPPENED? 표가 원래도 기준일 하나로 전일/전주/전월/전분기 대비를 전부 동시에 보여주므로, DoD/WoW/MoM/QoQ를 "기준일-이동 프리셋"에서 제거하고 "단일 일자"/"기간 범위" 두 모드를 프리셋 하나로 통합했다: **오늘(최신) / 어제(신규, 오늘 바로 아래) / 지난 7일 / 지난 1달 / 연간(1월 1일~오늘) / 직접 선택**. "어제"처럼 최신일이 아닌 하루를 볼 때는 "오늘" 대신 "이 날(YYYY-MM-DD)"로 서술해 같은 종류의 오표기가 재발하지 않도록 했다. 상세는 DATA_DICTIONARY.md §11.

**2026-08-20 DoD/WoW/MoM/QoQ/YoY를 "비교 분석" 프리셋으로 재도입(같은 날 네 번째 사용자 지시)**: 바로 위에서 뺐던 DoD/WoW/MoM/QoQ를 사용자가 지정한 순서로 다시 넣되, 이번엔 "기준일 이동"이 아니라 **달력 기준 "이번 기간 vs 전 기간" 명시적 비교**로 구현했다 — 최종 프리셋 순서: 오늘(최신) → 어제 → 지난 7일(종합 데이터) → 지난 1달(종합 데이터) → 연간(종합 데이터) → 직접 선택 → **전일 대비 오늘 분석(DoD)** → **전주 대비 이번주 분석(WoW)** → **전월 대비 이번달 분석(MoM)** → **전분기 대비 이번분기 분석(QoQ)** → **전년 동기 대비 이번년도 누적 분석(YoY)**. WoW는 "이번주"(이번주 월요일~오늘)를 "전주의 같은 요일 구간"과 비교하는 식으로 달력 주/월/분기/년 경계에 정확히 맞춰 이번/전 기간을 계산한다(단순 트레일링 윈도우와 다름). 상세는 DATA_DICTIONARY.md §12.

---

## 2. 데이터 흐름 (입력 → 처리 → 출력)

```
[입력]
  관리자가 Excel 업로드           Nielsen 메일 자동 수신 (2단계)
  (Nielsen 일별파일, skyUHD,       ┌─────────────────────────┐
   목표 시청률, 채널정보/로고) ──▶│  같은 처리 과정을 탄다      │
        │                        └─────────────────────────┘
        ▼
[처리 — ①원본 보관]
  업로드된 파일을 원본 그대로 보관 (수정하지 않음)
        │
        ▼
[처리 — ②검증]
  파일명·시트 구성·값이 정상인지 자동 확인
  문제 있으면 🔴 경고 표시하고 다음 단계로 넘어가지 않음
        │
        ▼
[처리 — ③정리해서 저장]
  다단 구조로 되어있는 Excel을 표 형태로 풀어서
  Supabase(데이터베이스)에 정리해서 저장
  (채널 로고 대표 색상도 이 단계에서 추출해 Channel Master에 저장)
        │
        ▼
[처리 — ④계산]
  PD가 화면을 보거나 질문을 하면, Claude가 그때그때
  SQL로 데이터베이스에 직접 계산을 시켜서 정확한 숫자를 받아옴
  (Claude가 암산으로 숫자를 만들지 않음)
        │
        ▼
[처리 — ⑤해석]
  Claude가 그 숫자가 무슨 의미인지 문장으로 설명
  (왜 올랐는지, 무엇을 추천하는지 등)
        │
        ▼
[출력]
  Page 1 종합 대시보드 (매일 아침 자동 생성, 중립 톤)
  Page 2 채널별 딥다이브 (PD가 채널 선택 시 그 채널 색으로 표시)
  자연어 질문 답변 (채팅으로 바로 응답)
```

**핵심 원칙**: 숫자 계산(④)은 반드시 데이터베이스가 하고, 문장 설명(⑤)만 Claude가 한다. 이렇게 나눠야 계산 오류가 없다.

---

## 3. 기술 선택

기본은 **Next.js**로 고정한다 (화면과 서버 코드를 한 프로젝트에서 처리하고, 3일차에 Vercel로 그대로 배포하기 위함). 그 위에 아래 기술들이 추가로 필요하다.

| 기술 | 한 줄 설명 |
|---|---|
| **Supabase** (이미 결정됨) | 시청률 같은 데이터를 저장해두는 온라인 데이터베이스 — 엑셀 파일 대신 여기에 정리해서 보관 |
| **엑셀 읽기 도구** (`xlsx` 라이브러리) | 업로드된 Nielsen 엑셀 파일을 프로그램이 읽고 이해할 수 있게 해주는 도구 |
| **Claude API 연결** (Anthropic SDK) | 우리 서비스 안에서 Claude가 직접 질문에 답하고 브리핑 글을 쓸 수 있게 연결해주는 통로 |
| **로그인 기능** (NextAuth 또는 간단한 자체 구현) | 관리자 2명이 아이디·비밀번호로 로그인할 수 있게 해주는 기능 |
| **그래프 그리기 도구** (Recharts) | 시청률 추이를 막대그래프·꺾은선그래프·미니 차트로 화면에 그려주는 도구 |
| **화면 스타일 도구** (Tailwind CSS) | 참고 이미지 같은 라운드 카드·그라디언트·그림자 스타일을 코드로 일관되게 입혀주는 도구 |
| **로고 색상 추출 도구** (색상 분석 라이브러리) | 채널 로고 이미지 파일에서 대표 색상을 자동으로 뽑아내, 그 채널 화면의 테마 색으로 쓰는 도구 |
| **자동 실행 스케줄러** (Vercel Cron) | "매일 아침 9시에 브리핑을 자동으로 만들어라" 같은 걸 정해진 시간에 자동 실행시켜주는 기능 |
| **이메일 자동 수집** (Gmail API, 20단계 완료) | Nielsen 메일함(Bizbox 그룹웨어, 2FA)은 직접 자동 로그인이 안 돼, Gmail로 전달받은 메일을 Gmail API(OAuth2)로 읽어 첨부파일을 가져온다 — Vercel Cron이 매일 정해진 시각에 실행 |

> 이 목록은 지금 시점의 제안이며, 실제 개발하면서 더 간단하거나 잘 맞는 대안이 보이면 바뀔 수 있다 (CLAUDE.md 원칙: 코드를 바꾸면 무엇을 왜 바꿨는지 알려드림).

---

## Version History

| Version | Date | Changes |
|---|---|---|
| 0.1 | 2026-08-19 | PLAN.md 기준 최초 작성 (화면 구성·데이터 흐름·기술 선택) |
| 0.2 | 2026-08-19 | 참고 이미지 톤앤매너 반영 — Page 1은 중립 파스텔 톤, Page 2는 채널 로고 색상 테마 적용 |
| 0.3 | 2026-08-19 | 개발 단위 20번(Nielsen 메일 자동 수집) 완료 — 이메일 자동 수집 방식을 Gmail API(OAuth2) 전달 방식으로 확정 |
| 0.4 | 2026-08-19 | Page 1 레이아웃을 사용자 지시로 2열×3행 그리드 + 참고 이미지(SYNAPSE) 톤앤매너로 재설계 |
| 0.5 | 2026-08-20 | Page 2를 카드 나열에서 보고서 줄글+표 형태로 재설계(사용자 지시), COMPARED WITH? 막대그래프를 순위표+줄글로 대체, 우측 상단 기간 설정을 DoD/WoW/MoM/QoQ/YTD/직접 선택으로 구체화. 상세 설계 판단은 DATA_DICTIONARY.md §9. |
| 0.6 | 2026-08-20 | Page 2 기간 설정을 단일 일자/기간 범위(YYYY-MM-DD~YYYY-MM-DD) 토글로 확장(사용자 지시) — 기간 범위 선택 시 브리핑~COMPARED WITH? 대부분 재계산. 02~26시 시간대별 그래프를 오늘의 브리핑 바로 아래로 이동하고, 최근 12주 요일×시간대 강세 히트맵 + 시청률 상위 콘텐츠 TOP 20 섹션 신규 추가. 상세 설계 판단은 DATA_DICTIONARY.md §10. |
| 0.7 | 2026-08-20 | DoD/WoW가 기준일을 과거로 옮겨 "오늘"을 잘못 서술하던 버그 수정(사용자 지시) — DoD/WoW/MoM/QoQ를 기준일-이동 프리셋에서 제거하고 오늘/어제/지난 7일/지난 1달/연간/직접 선택으로 프리셋을 하나로 통합, "이 날(YYYY-MM-DD)" 라벨 도입. 상세는 DATA_DICTIONARY.md §11. |
| 0.8 | 2026-08-20 | DoD/WoW/MoM/QoQ/YoY를 "달력 기준 이번 기간 vs 전 기간" 비교 분석 프리셋으로 재도입(사용자 지시, 지정 순서 그대로) — 지난 7일/1달/연간(종합 데이터)과 구분되는 5개 프리셋 추가. 상세는 DATA_DICTIONARY.md §12. |
