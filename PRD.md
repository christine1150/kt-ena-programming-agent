# KT ENA 편성 AI Agent 서비스 기획서 (PRD)

- 작성자: [이름]
- 작성일: (오늘 날짜)

---

## 1. 추진 배경 (문제 정의)
*누가, 어떤 업무에서, 어떤 불편을 겪는가*

본 프로젝트는 Nielsen 시청률 데이터를 기반으로 채널·프로그램·시간대·타깃·경쟁환경·목표 달성 현황을 자동 분석하고, 편성 의사결정을 지원하는 다채널 Audience & Programming Intelligence AI 에이전트를 구축하는 것을 목적으로 한다.

핵심 철학은 별도의 SQL/Python 분석 엔진 없이, Claude AI가 DB에 정리·저장된 데이터를 직접 조회해 정해진 계산 공식에 따라 수치를 산출하고, 그 결과의 의미까지 해석해 편성 의사결정을 지원하는 것이다. 즉 데이터 조회·계산·해석 전 과정을 Claude가 수행하되, 계산은 항상 사전에 정의된 고정 공식과 DB에 정제·검증되어 저장된 데이터만을 근거로 하며, 원본 Excel 파일을 직접 열어 임의로 계산하거나 존재하지 않는 수치를 추정하지 않는다.

이 서비스를 사용하는 사람은 KT ENA 채널의 편성 PD다. 이들은 "어제 시청률이 얼마였는가?"를 아는 데서 그치지 않고, "무슨 일이 있었는가 → 왜 그런가 → 누가 시청했는가 → 목표 대비 어떤가 → 경쟁채널은 어떤가 → 어디에 기회가 있는가 → 무엇을 편성해야 하는가"까지 매일 스스로 데이터를 취합·정리·계산·비교해야 하는 업무 부담을 안고 있으며, 그 과정에서 시간 소요와 수작업 오류 가능성이라는 불편을 겪고 있다.

---

## 2. 현행 방식과 한계
*지금은 이 일을 어떻게 처리하고 있고, 무엇이 아쉬운가*

현재는 담당자가 Nielsen Excel을 매일 수신해 여러 Sheet를 직접 확인하고, 채널별·프로그램별·시간대별로 데이터를 수작업으로 정리한 뒤 전일·전주·전월·전년 대비, 경쟁채널 대비, 성별·연령별 타깃 대비 수치를 계산해 일일 보고서를 작성하고 있다.

이 방식은 다음과 같은 한계가 있다.
- 반복적인 데이터 가공에 많은 시간이 소요된다.
- 수작업 계산·복사 과정에서 오류가 발생할 가능성이 있다.
- 담당자에 따라 분석 기준이 달라질 수 있다.
- 단순 데이터 정리에 시간을 많이 써서, 정작 경쟁환경·타깃·시간대까지 종합적으로 분석하기 어렵다.
- 분석 결과가 실제 편성 의사결정으로 연결되기까지 시간이 걸린다.

---

## 3. 목표와 기대효과 (성공 기준)
*측정 가능한 숫자로 (처리 시간, 문의 건수, 정답률 등)*

- 일일 데이터 취합·정리·보고서 작성 시간을 기존 대비 80% 이상 단축한다 (예: 약 2시간 → 20분 이내).
- Morning Briefing을 매일 오전 9시 이전 자동 생성한다 (수동 작성 0건).
- PD가 Morning Briefing 첫 화면만으로 핵심 상황(무슨 일이 있었는가 ~ 무엇을 편성해야 하는가)을 쉽게 파악할 수 있게 한다.
- 목표 달성률·Gap 등 핵심 수치의 계산 오류율 0%를 유지한다 (모든 수치는 Claude가 DB의 정제된 데이터를 바탕으로 고정된 공식에 따라 직접 계산).
- 자연어 질문에는 항상 DB에 저장된 검증된 데이터만 근거로 답변하며, 근거 데이터가 없으면 추측하지 않고 "찾을 수 없습니다" 등으로 명확히 안내한다.
- 편성 추천 결과에 근거(Evidence)와 신뢰도(Confidence)를 표시하는 비율 100%를 유지한다.

---

## 4. 사용자와 이용 흐름
*누가 쓰는지와 사용 순서 (화살표로)*

KT ENA 편성 PD → 출근 후 Morning Briefing 확인 (WHAT HAPPENED? → WHY? → WHO IS WATCHING? → HOW DEEPLY? → CONTENT FITS? → COMPARED WITH WHAT? → WHERE IS THE OPPORTUNITY? → WHAT SHOULD WE SCHEDULE?) → Portfolio Health / 타깃 변화 / 경쟁채널 현황 파악 → 궁금한 부분은 자연어로 추가 질문 (Conversational BI) → 편성 추천(KEEP / MOVE / REPLACE / STRENGTHEN / TEST)과 근거·신뢰도 검토 → 실제 편성 회의·의사결정에 참고 자료로 활용

---

## 5. 주요 기능 (must / nice로 구분)

### Must

**1) Nielsen 데이터 자동 집계 · 분석 · Morning Briefing · 편성 추천**
- 설명: 아래 기능을 하나의 파이프라인으로 자동 수행합니다. (우선순위: 자동 데이터 수집 → 시청률/목표 대비 분석 → Morning Briefing → 편성 추천을 1차로 구현하고, 타깃 Affinity·경쟁채널·프로그램 유형 분석은 데이터가 충분히 쌓인 이후 순차 확장한다.)
  - 자동 데이터 수집: 2026년 1월~8월 17일 데이터는 관리자가 Daily Excel 파일을 직접 업로드해 테스트한다 (2026-01-01~08-17 일별 파일이 3월 14일 하루만 빼고 모두 확보 완료 — 바로 업로드 테스트 가능). 별도로 2025년 연간 집계 파일(`닐슨_채널시청률(250101-251231).xls`)도 확보되어 있으며, 이는 일별 데이터가 아니라 **2025년 1~12월 전체를 합친 연간 집계값**이므로 DoD/WoW 등 일별 비교에는 쓰지 않고, **YoY(전년 대비) 비교 기준값 및 2026년 목표 시청률 설정 근거 자료**로 별도 관리한다 (일단은 연간 집계 형태 그대로 보관하고, 추후 2025년 일별 데이터가 준비되면 2026년과 동일하게 Daily 단위로 별도 업로드할 예정). 추가로 `채널기본정보.xlsx`의 `연간 채널별 가구 시청률`/`연간 채널별 수도권 2049 시청률` 시트에 **2017~2025년 채널별 연간 시청률·순위 추이**가 더 폭넓게 정리되어 있어, 장기 트렌드 참고 자료로 함께 활용한다. 이 과정에서 Channel Master 등 기준 데이터도 함께 등록한다. 이후(2026년 8월 18일~)부터는 매일 오전 8시 이후 christine@ktena.co.kr로 수신되는 "[닐슨] KTENA 일일 보고서 (YYMMDD)" 메일에서 "닐슨_채널시청률(YYMMDD).xls" 첨부파일을 자동으로 찾아 업로드·반영한다 (메일함 접근 인증 방식은 추후 함께 논의해 결정한다) (Email → Excel → RAW → DB). 실제 샘플 파일(260816, 260817) 확인 결과, 파일은 `유료방송가입가구`/`개인`/`OOO경쟁채널시청률`(4종)/`OOO타깃상세`(4종) 총 10개 시트로 고정 구성되며, 각 시트는 타깃·채널별 블록이 가로로 반복되는 다단 헤더 구조다.
  - 채널 관리: 분석 대상 채널은 ENA, ENA Drama, ENA Play, ENA Story, OLIFE, ONCE 총 6개 채널이며, 시청률·목표대비·타깃·경쟁채널·프로그램 분석을 모두 수행한다. skyUHD는 별도로 시청률 데이터만 업데이트해 관리하며, 위 6개 채널과 같은 수준의 전체 분석 대상에는 포함하지 않는다 (총 7개 채널을 Channel Master에서 관리). 채널 목록은 코드에 고정하지 않고, 관리자가 업로드하는 Channel Master Excel 파일로 등록·관리한다. Channel Master에는 채널명 외에 시장구분(수도권/전국)·Primary Target·프라임타임(초기엔 2026년 데이터 분석으로 자동 산출, 이후 관리자가 재정의 가능) 속성도 포함한다. 시장구분·Primary Target 값은 `채널기본정보.xlsx`로 확보 완료 (아래 "목표 대비 분석" 항목 참고). 채널 로고 이미지도 `채널 로고/` 폴더에 7개 채널 전부 확보되어 있어 화면에 활용한다 — **화면에 표시할 때는 ENA 로고의 높이를 기준으로, 다른 채널 로고의 최대 높이도 동일하게 맞춰 표시한다.** KT ENA 회사 CI 로고는 추후 별도 전달 예정.
  - skyUHD 데이터 소스: 나머지 6개 채널과 달리 Nielsen 표준 포맷이 아니라, 관리자가 수기로 계속 업데이트하는 별도 엑셀 파일로 관리한다. 프로젝트 폴더 내 `Nielsen Data/2026/skyUHD/` 경로로 추후 업데이트될 예정 (현재는 미도착, 도착 시 확인). 업데이트될 때마다 기존 파일을 덮어쓰지 않고 **새 파일명(`skyUHD 시청률 (MMDD).xlsx`)으로 추가** 등록하는 방식이다. 이 파일의 `26 UHD ALL` 시트(날짜·요일·시작시간·끝시간·프로그램명·시청률로 구성된 단순 표)를 파싱 기준으로 삼는다 — 월별 시트는 사람이 보기 위한 것이라 파싱하지 않는다. 시작/끝시간은 `시:분:초:프레임`(예: `05:02:20:38`) 형식으로, 메인 Nielsen 파일의 `시:분:초` 형식과 다르다. 프레임 값은 fps로 환산하지 않고 **무시(절삭)**하여 `시:분:초`까지만 사용한다. skyUHD는 시청률(Rating)만 존재하고 Share/Reach/Time Spent/타깃 세그먼트는 없으므로, 시청률 기준 랭킹·모니터링 용도로만 사용한다.
  - 시청률 분석: Rating, Share, Reach, Time Spent, Time Spent Share의 평균·합계, DoD/WoW/MoM/QoQ(일반 캘린더 분기: 1~3월/4~6월/7~9월/10~12월)/YoY 비교 및 WTD(주초 누적)/MTD(월초 누적)/QTD(분기초 누적)/YTD(연초 누적) 누적 지표
  - 콘텐츠 관리: 주요 프로그램은 두 가지 방식으로 관리한다. ① **킬러 콘텐츠** — 채널별 최근 4주 평균 시청률 상위 3~5개 프로그램을 자동으로 산출한다. ② **주요 콘텐츠** — 관리자가 직접 지정하며 카테고리로 분류하고, 본방송 시간을 함께 관리한다. `채널기본정보.xlsx`(`KT ENA 오리지널` 시트)에 실제 콘텐츠 목록(85건)이 이미 확보되어 있으며, 카테고리는 이 시트에 나타난 세분화된 기준(오리지널 드라마·독점 예능·오리지널 예능·브랜디드·구매 예능 등)을 그대로 사용한다. 본방송 시간은 "2026.02.02. 매주월화 밤 10:00"처럼 자유 텍스트로 기록되어 있어, 파싱해서 `시작일(nullable)`·`종료일(nullable=계속 방영중)`·`요일(배열)`·`시각(24시간제)` 구조화 필드로 분리한다. 시작일이 텍스트에 없는 경우(예: "매주 월화 밤 10:00"만 있는 경우) Nielsen 실제 방영 데이터에서 해당 프로그램명이 처음 등장한 날짜로 역산하며, 그래도 파싱에 실패하면 원문을 그대로 보존하고 "파싱 필요" 플래그를 남긴다. 단, 이 시트의 채널별 편성정보는 **참고용 자료이며 실제로 편성되었어도 기입되지 않은 경우가 있으므로**, 값이 채워진 행만 신뢰하고 비어있는 셀을 "해당 채널에 편성되지 않음"으로 단정하지 않는다.
  - 화면 구성: **Page 1(종합 대시보드, Portfolio Executive View)** — 7개 채널 전체의 당일 성과·시장 내 순위·프라임타임 주도권·주요 프로그램 실적(킬러 콘텐츠 + 관리자 지정 주요 콘텐츠)을 한눈에 스캐닝. **초안(검토용, 추후 수정 가능)**은 아래 4단 구조:
    - ① **ENA 채널 하이라이트** (최상단, 최우선) — 순위·시청률(수도권2049 기준, 유료가구 병기), 오리지널 드라마 당일/최근 본방 실적 + 동시간대 경쟁채널 비교, 오리지널 예능 본방 실적(수도권2049+유료가구 병기, 동시간대 경쟁채널 비교)
    - ② **ENA Play / ENA Drama** — 각 채널 요약 실적, ENA 본방과 동시/직후 재방한 오리지널의 실적, 타깃별 시청률 + 동시간대 경쟁사 비교, 기타 서머리
    - ③ **OLIFE·ONCE·ENA Story(유료가구 채널 묶음)** — 3개 채널을 카드 형태로 나란히 배치, 각각 순위·시청률·주요 프로그램 요약
    - ④ **포트폴리오 통합 그래프** — 서로 다른 KPI(수도권2049 vs 유료가구)를 가진 채널을 한 그래프에 비교하기 위해 원값 대신 목표 달성률(%) 또는 순위 등 공통 기준으로 정규화해 도식화

    채널별 "프라임타임"은 고정된 시간대(예: 19~23시)로 하드코딩하지 않고, 2026년 실제 시청률 데이터를 분석해 **채널마다 개별적으로** 산출한 시간대를 기본값으로 사용한다. 이후 관리자가 Channel Master에서 채널별 프라임타임을 직접 재정의할 수 있게 한다. **Page 2(채널별 딥다이브, Channel Deep-Dive View)** — 좌측에서 채널 하나를 선택하면 아래 구조로 구성된 상세 화면을 보여준다:
    - **헤더**: 채널 로고, 기간 라벨(DoD/WoW/MoM/QoQ/YTD 중 체크박스로 직접 전환 가능, 기본값 당일), 시청률(순위)과 전일 대비 증감(예: "0.132 (32위) ▲3"), 목표 대비 연간 누적 성과(등위 포함)
    - **본문을 8대 질문으로 섹션화**: WHAT HAPPENED?(DoD/WoW/MoM/QoQ/YTD 성과 추이) → WHY?(타깃 구성 변화·프로그램 성과·경쟁채널 동향 등 변동 요인) → WHO IS WATCHING?(성·연령 및 복합 타깃, Audience DNA, Affinity) → HOW DEEPLY?(Rating/Share/Reach 및 Time Spent/Time Spent Share) → CONTENT FITS?(특정 타깃·시간대에 맞는 콘텐츠 장르·유형 매칭) → OPPORTUNITY?(Target Demand↑ & 경쟁 압력↓ 기회 슬롯) → WHAT TO SCHEDULE?(편성 추천) → COMPARED WITH?(전일/전주/전년 동기 및 지정 경쟁채널 대비 Gap 분석)
    - **시간대별 프로그램 그래프**: 02시~26시 축, 체크박스로 지표(시청률/점유율/도달율/시청시간) 선택 가능, 기본값은 해당 채널의 KPI 시청률
    - **경쟁 채널 리포트**: 경쟁사 순위·Gap, 동시간대 경쟁 프로그램 비교
    
    기본은 당일 데이터 기준이며, 헤더의 기간 설정을 바꿔 비교할 수 있다. 자연어 질문 기능(기능 2)은 별도 화면이 아니라 **Page 2의 "WHAT TO SCHEDULE?" 섹션 아래(자연어 검색)에 하나의 섹션**으로 배치한다.
  - 원인 추적(Root-Cause 참고 분석): 시청률 이상 변화(1차 단순 기준: 채널 평균 대비 -10%p 이상 하락이 **3일 이상 지속**, 운영하며 PD 피드백으로 조정 예정 — 하루짜리 변동은 노이즈로 보고 트리거하지 않음)가 감지되면 동시간대 경쟁채널의 편성 변화를 자동으로 함께 조회해 제시한다. **편성 변화 감지 기준**: 최근 2주 동일 요일·동일 시간대 편성 이력과 비교해 ①주요 시청률 획득 프로그램이 바뀌었거나 ②편성 시간·프로그램이 최근 2주 패턴과 달라졌거나 ③신규 프로그램이 편성됐거나 ④기존 편성 프로그램이 빠진 경우 중 하나에 해당하고, 그로 인해 해당 시간대 시청률이 5% 이상 하락 또는 상승했을 때 "편성 변화"로 판단한다. 단, "시청자가 이동했다"거나 특정 비율(%)로 유입·이탈했다고 단정하지 않고, "동시에 관찰됨 · 상관관계 가능성이 있으나 인과관계로 단정할 수 없음" 형태로만 참고 정보로 제공한다 (6번 비범위의 Audience Flow 추적 금지 원칙 준수).
  - 기회 탐지(Opportunity Alert): 원인 추적과 반대로, **경쟁채널의 약세(하락)와 자사 콘텐츠의 강세(상승)가 같은 시간대에 겹치는 경우**를 자동으로 탐지해 "기회 슬롯"으로 제시한다. 마찬가지로 인과관계를 단정하지 않고 참고 정보로만 제공한다.
  - 목표 대비 분석: 목표 시청률 대비 달성률·Gap (예: ENA PLAY 목표 0.30, 실제 0.34, 달성률 113.3%, Gap +0.04). 정식 목표값이 `채널기본정보.xlsx`(`채널 별 경쟁채널` 시트)로 확보되어, 아래 값을 목표 시청률·목표 등위로 사용한다 (KPI 시청률 = Primary Target, 시장구분은 KPI 값에서 유도: "수도권 개인2049"→수도권, "National 유료방송가입가구"→전국):
    - ENA: KPI 수도권 개인2049, 목표 6위, 목표 시청률 0.18367
    - ENA Drama: KPI 수도권 개인2049, 목표 35위, 목표 시청률 0.02554
    - ENA Play: KPI 수도권 개인2049, 목표 30위, 목표 시청률 0.03032
    - ENA Story: KPI National 유료방송가입가구, 목표 80위, 목표 시청률 0.04395
    - OLIFE: KPI National 유료방송가입가구, 목표 44위, 목표 시청률 0.0925
    - ONCE: KPI National 유료방송가입가구, 목표 60위, 목표 시청률 0.0601
    - skyUHD: KPI National 유료방송가입가구, 목표 2위 (경쟁채널 5개를 포함한 총 6개 UHD 채널 중), 목표 시청률 0.0019
  - 타깃 분석: 2049·성별·연령 등 세그먼트별 Affinity, Contribution
  - 경쟁채널 분석: Rank, Gap, Competitive Pressure, Opportunity. Competitive Pressure(경쟁 압력, 0~100)는 "동시간대 상위 3개 경쟁채널 평균 시청률 ÷ 자사 프로그램 시청률 × 100"으로 계산하며 100 초과 시 100으로 클램프한다 (값이 클수록 경쟁이 치열함을 의미). Competitor Master 전체 목록이 `채널기본정보.xlsx`로 확보됨:
    - ENA: tvN, SBS, MBC, KBS1, KBS2, JTBC, 채널A, TV CHOSUN, MBN
    - ENA Play: MBC every1, SBS funE, SBS Plus, DRAMAcube, Dramax, tvN SHOW, tvN STORY, MBC드라마넷, E채널, 코미디TV, theLIFE, KBS JOY, 채널S, Mnet (+ 내부 비교채널: ENA)
    - ENA Drama: SBS Plus, MBC드라마넷, KBS드라마, tvN STORY, tvN DRAMA, JTBC2, DRAMAcube, Dramax (+ 내부 비교채널: ENA)
    - ENA Story: D-ONE, FUN TV, K STAR, GTV, CMCTV, theLIFE, CH view, 코미디TV
    - OLIFE: CMCTV, ONT
    - ONCE: EDGETV, CNTV, D-ONE, MBC ON, K STAR, CH view
    - skyUHD: UXN, ASIA UHD, UMAX, UHD Dream TV, SBS F!L UHD
    - 단, 원본 Nielsen 파일의 `OOO경쟁채널시청률` 시트는 채널당 고정 1개 비교 채널만 제공하므로(예: ENA Drama 시트엔 ENA STORY만 존재), 위 전체 Competitor Master 기반 순위·Gap 계산은 `유료방송가입가구`/`개인` 전체 채널 랭킹 시트에서 각 경쟁채널을 찾아 집계하는 방식으로 구현한다.
  - 프로그램 분석: Top/Bottom, Audience DNA, Target Affinity, Acquisition/Retention/Mass/Core 유형 분류
  - Morning Briefing 자동 생성 (매일 오전): 아래 6개 섹션으로 구성한다.
    - ① Daily Channel Health Check — 채널별 상태를 Strong/Stable/Watch/Critical 4단계로 요약
    - ② Daily Performance Intelligence — 시간대별·프로그램별 강세·약세 Top/Bottom
    - ③ Competitive Intelligence — 경쟁채널 대비 상대적 위치, Competitive Pressure Score
    - ④ Content/Program Asset Intelligence — 장르·타깃 중심 콘텐츠 생산성 평가 (킬러 콘텐츠·주요 콘텐츠 실적 포함)
    - ⑤ Audience Flow Intelligence — Lead-in/Lead-out 및 블록 편성 효과 참고 분석 (개인 단위 이동 단정 금지, 6번 비범위 원칙 준수)
    - ⑥ Scheduling Intelligence — 편성 추천(Fit Score/Confidence 포함)
  - 편성 추천: KEEP, MOVE, REPLACE, STRENGTHEN, TEST 중 하나를 근거·Fit Score·Confidence와 함께 제시한다. Program × Slot × Target 단위로 아래 **Fit Score(0~100)**를 계산해 판단 근거로 삼는다 (최근 12주 이상 데이터가 이미 확보되어 있어, 아래 규칙을 처음부터 적용한다):
    - **Fit Score 구성**: 30% Target Performance + 20% Target Affinity + 15% Audience Engagement + 15% Slot Performance + 10% Competitive Opportunity + 10% Audience Flow (모든 하위 점수는 먼저 0~100으로 표준화한 뒤 가중합)
      - **Target Performance** = 40%×Program Target Rating Score + 30%×Same Slot Historical Score + 30%×Same Daypart Score — 각각 "최근 12주 동일 요일·시간대·타깃" 기준 자사 채널 내 percentile로 0~100 환산
      - **Target Affinity** = Affinity Index(기존 정의 공식)를 최근 12주 percentile로 0~100 환산
      - **Audience Engagement** = 50%×Reach Score + 50%×Time Spent Share Score (각각 percentile 환산). Reach=얼마나 넓게 유입됐는가, Time Spent=얼마나 오래 봤는가, Time Spent Share=어떤 타깃이 집중해서 봤는가로 역할을 분리해 해석한다.
      - **Slot Performance** = 50%×최근 12주 동일 요일×동일 시간대 percentile + 30%×동일 Daypart percentile + 20%×동일 채널 해당 슬롯 최근 추세
      - **Competitive Opportunity** = 40%×(100−Competitive Pressure) + 30%×경쟁채널 대비 Gap 정규화 + 30%×최근 경쟁강도 추세
      - **Audience Flow** = Lead-in Retention(= 다음 프로그램 Target Rating ÷ 이전 프로그램 Target Rating)을 0~100으로 표준화. "시청자가 이동했다"고 해석하지 않고 앞뒤 프로그램 간 시청률 유지 비율로만 다룬다 (6번 비범위의 Audience Flow 추적 금지 원칙 준수).
    - **태그 결정(기존 5개 태그 체계 유지)**:
      - 표본 부족으로 Confidence가 낮으면 Fit Score와 무관하게 **TEST**로 표시하고 "탐색적 추천"으로 안내한다.
      - Fit Score 80~100 → **STRENGTHEN**
      - Fit Score 65~79 → **KEEP**
      - Fit Score 50~64 → **MOVE** (다른 시간대 재배치 검토)
      - Fit Score 50 미만 → **REPLACE** 검토
    - Fit Score와 Confidence는 반드시 함께, 별개 지표로 표시한다 (예: "Fit Score 93 / Confidence 38%(표본 6건)" → 점수가 높아도 표본이 적으면 탐색적 추천으로 안내).
    - **구현 방식**: Fit Score는 Claude가 암산하지 않고 SQL/Python으로 사전 계산된 MART 테이블(`MART_PROGRAM_TARGET_SCORE`, `MART_SLOT_SCORE`, `MART_COMPETITIVE_SCORE`, `MART_FLOW_SCORE` → `MART_SCHEDULING_FIT_SCORE`)을 조회해 사용하며, Claude는 결과를 해석해 추천 이유를 설명하는 역할만 담당한다. 가중치(30/20/15/15/10/10)는 코드에 고정하지 않고 CONFIG 테이블로 관리해 운영 데이터로 채널별 튜닝이 가능하게 한다.
    - 데이터가 충분히 쌓인 이후에는 Confidence 외에 **예상 시청률 Range**(예: "0.25~0.35% 예상")도 함께 제공한다.
- AI가 꼭 지켜야 할 규칙:
  - Claude는 DB(DW)에 정리·저장된 데이터를 직접 조회해 정해진 공식에 따라 수치를 계산하며, 원본 Excel 파일을 직접 열어 파싱하거나 임의로 계산하지 않는다.
  - 목표 달성률은 "실제 평균 시청률 ÷ 목표 시청률 × 100", Gap은 "실제 − 목표"로 고정 계산한다.
  - Affinity는 "특정 Target 구성비 ÷ 비교 기준 채널의 해당 Target 구성비 × 100"으로 계산하며, Affinity ≠ 시청률이고 최소 표본·최소 분모 조건을 만족해야 표시한다.
  - 경쟁채널은 Competitor Master에 등록된 채널만 사용하며, Claude가 경쟁채널이나 존재하지 않는 데이터를 임의로 생성하지 않는다.
  - 관리자 2명은 로그인 후 Nielsen 백업 업로드, 목표 시청률, Channel Master 업로드 및 전체 기능을 사용할 수 있다. 그 외 PD는 관리자가 발급한 고정 링크 하나를 공유해 접속해 Morning Briefing 열람과 자연어 질문 기능만 사용할 수 있으며, 업로드 권한은 없다 (개별 사용자 식별은 하지 않음).
  - 모든 계산 공식은 본 PRD의 AI 규칙에 우선 명시하며, 추후 DATA_DICTIONARY.md에 상세 계산 기준을 별도로 문서화해 관리한다.
  - 핵심 수치(합계·평균·달성률·Gap·Affinity 등)는 Claude가 직접 암산하지 않고 SQL 쿼리(집계 함수)를 실행해 계산함으로써 정확성을 보장하며, 복잡한 통계 계산이 필요한 경우 Python 분석 스크립트를 실행할 수 있다. 이 경우에도 계산 로직은 항상 사전에 정의된 고정 공식을 따른다.
  - Acquisition/Retention/Mass/Core 등 콘텐츠 유형은 콘텐츠의 절대적 속성으로 단정하지 않고, "최근 12주 데이터 기준" 등 분석 기간을 함께 표시한다.
  - NULL(데이터 없음)은 0으로 임의 변환하지 않고 구분해서 표시한다.
  - 데이터 품질 검증(파일·구조·값·완전성)에서 심각한 오류가 발견되면 🔴 DATA QUALITY ALERT를 먼저 표시하고 해당 분석을 중단한다. 구조 검증에는 신규 컬럼 추가·기존 컬럼 변경 등 **Nielsen 파일 스키마 변경 감지**를 포함한다.
  - 요일·시즌에 따라 시청률 편차가 크므로(주말 vs 평일, 방학·명절·스포츠 경기 등), 단순 DoD 비교보다 **동일 요일·동일 시간대 기준 WoW/YoY 비교**를 우선한다.
  - Morning Briefing이나 자연어 질문에 대한 설명은 **What(무슨 일이 있었는가) → Why(왜 그런가) → So What(그래서 어떤 의미인가) → What Next(다음에 무엇을 해야 하는가)** 순서로 서술해, 숫자 나열이 아니라 현상 파악부터 액션 제안까지 이어지는 논리 흐름을 갖춘다.
  - Morning Briefing은 당일 Nielsen 데이터 반영이 완료된 뒤 자동 생성되며, Portfolio Health(🟢/🟡/🔴)를 최상단에 배치해 PD가 핵심 상황을 쉽게 파악할 수 있게 한다.
  - 편성 추천을 제시할 때는 반드시 근거(Evidence)와 Confidence(신뢰도)를 함께 표시하며, 표본이 부족하면 "INSUFFICIENT SAMPLE"로 표시하고 단정하지 않는다. Confidence는 숫자(0~100%)와 등급을 함께 표시한다 (예: "72% (Medium)") — 1차 기준: 80% 이상 High, 50~79% Medium, 50% 미만 Low, 운영하며 조정 예정.
  - 시청률 변화의 원인을 설명할 때 상관관계를 인과관계로 단정하지 않는다.
  - 프로그램 실적을 집계할 때는 회차 번호와 `<본>`(본방송) 표시를 제거한 정규화된 프로그램명 기준으로 합산한다. 단, "본방송 시청률"처럼 본방송만 콕 집어 질문하면 `<본>` 표시가 있는 회차만 따로 필터링해 답변한다. 회차 정보(원본 프로그램명·회차 번호)는 삭제하지 않고 그대로 보존한다. 경쟁채널의 편성 변화를 함께 제시할 수는 있지만, "시청자가 몇 % 이동했다"처럼 개인 단위 이동을 단정하는 표현은 사용하지 않는다 (6번 비범위 참고).

**2) 자연어 질문 기능 (Conversational BI)**
- 설명: PD가 자연어로 질문하면, Claude가 질의를 SQL로 변환(NL2SQL)해 DB의 검증된 데이터를 조회하고 답변합니다. 예: "최근 4주 ENA PLAY 2049가 상승했는데 어떤 연령대가 기여했어?", "ENA DRAMA와 경쟁채널(SBS Plus, MBC 드라마넷 등) 중 어제 2049 시청률 1위는 어디야?", "ONCE 대비 경쟁사인 MBC ON, KBS Story의 최근 4주간 수요일 Prime 시간대 평균 점유율 보여줘.", "2월이랑 7월 비교해서 시청률 많이 오른 프로그램 순서대로 보여줘"(임의의 두 기간을 지정해 프로그램별 평균 시청률 상승률 순으로 재정렬).
- AI가 꼭 지켜야 할 규칙:
  - Claude는 DB에 저장된 정제 데이터를 직접 조회하고 필요한 계산을 스스로 수행하며, 원본 Excel을 직접 열람하지 않는다.
  - 계산이 필요한 경우 기능 1)에서 정한 고정 공식을 동일하게 적용한다.
  - 존재하지 않는 데이터나 경쟁채널 정보를 임의로 생성하지 않는다.
  - 답변에는 근거가 된 기간과 표본 수를 함께 명시한다.
  - 상관관계를 인과관계로 단정하지 않으며, 데이터가 부족하면 "INSUFFICIENT SAMPLE"로 답한다.
  - 두 기간을 비교해 프로그램별 평균 시청률 **상승률**로 순위를 매길 때는 "(기간B 평균 − 기간A 평균) ÷ 기간A 평균 × 100" 공식을 쓰며, 기존 프로그램명 정규화 규칙(회차/`<본>` 통합)을 그대로 적용해 집계한다. 두 기간 중 한쪽이라도 방영 횟수가 **3회 미만**이면 "INSUFFICIENT SAMPLE"로 표시해 순위에서 제외하고, 기간A에 방영 이력이 아예 없는 프로그램(신규 편성)은 상승률을 계산할 수 없으므로 "신규 편성(비교 불가)"으로 별도 표시한다.

### Nice
- (이번 범위에는 없음 — Must 2개 기능에 집중)

---

## 6. 범위와 비범위
*이번 실습에서 만들 것 / 만들지 않을 것*

### 범위 (만들 것)
- Nielsen 데이터 자동 집계 · 분석 · Morning Briefing · 편성 추천
- 자연어 질문 기능 (Conversational BI)
- 업로드 기능(Nielsen Excel, 목표 시청률, Channel Master 등) 접근을 위한 관리자 로그인
- 주요 콘텐츠 관리 기능 (관리자가 오리지널 드라마·독점 예능·오리지널 예능·브랜디드·구매 예능 등 `채널기본정보.xlsx` 기준 세분화된 카테고리로 직접 지정, 본방송 시간 포함 관리)
- 사용자 접근 제어: 관리자 2명은 로그인해 전체 기능(업로드 포함)을 사용하고, 그 외 PD는 관리자가 발급한 고정 링크 하나를 공유해 접속해 Morning Briefing 열람과 자연어 질문 기능을 사용한다 (업로드 권한 없음, 개별 사용자 식별은 하지 않음). 링크가 외부로 유출된 경우를 대비해, 관리자가 기존 공유 링크를 무효화하고 새 링크를 재발급할 수 있는 기능을 포함한다.

### 비범위 (만들지 않을 것)
- 콘텐츠 비용·매출 데이터 기반 Content ROI 계산 (현재는 Nielsen 데이터만으로 Content Efficiency까지만 다루고, 비용/매출 연동은 하지 않음)
- 개인 단위 패널 이동 데이터 기반 실제 Audience Flow 추적 ("시청자가 A에서 B로 이동했다"는 식의 단정적 분석은 하지 않음)
- 채널 간 시청자 중복(Cannibalization)의 확정적 판별 (잠재 가능성 표시까지만, 실측 데이터 기반 확정 분석은 제외)
- 편성표를 실제로 변경·반영하는 기능 (AI는 추천만 하고, 실제 편성 확정/반영은 PD가 별도 시스템에서 수행)
- 회원가입/공개 계정 생성 기능 (초대·링크 기반 접근만 지원하며, 누구나 가입할 수 있는 회원가입 기능은 만들지 않음)

---

## 7. 보안·개인정보 검토
*사용 문서의 공개등급, 개인정보 여부, API 키 관리 방법*

- 공개등급: 사내 편성 전략과 직결되는 자료이므로 대외비(사내 한정)로 취급하며, 외부 공개·GitHub 공개 저장소 업로드를 금지한다.
- 개인정보 여부: Nielsen 시청률 데이터는 패널 개인 식별정보를 포함하지 않으며, 채널·프로그램·시간대·타깃(성별·연령 세그먼트) 단위로 집계된 데이터만 사용한다.
- API 키 관리: GITHUB_TOKEN, SUPABASE_ACCESS_TOKEN, VERCEL_TOKEN, OPENAI_API_KEY 등 비밀 값은 코드에 하드코딩하지 않고 `.env` 파일에만 보관하며, `.gitignore`에 `.env`와 `node_modules`를 등록해 GitHub에 올라가지 않도록 한다.

---

## 8. 기술 스택
*개발에 사용할 도구 모음 (이 과정에서는 Next.js로 고정)*

- Next.js — 화면과 서버 쪽 코드(API)를 한 프로젝트에서 처리
- Supabase (PostgreSQL 기반) — 데이터 저장소. Claude는 Next.js API Route를 통해 Supabase에 저장된 정제 데이터를 조회하고 계산한다 (구체적인 연동 방식은 개발 단계에서 편한 방식으로 정한다). DB 스키마는 처음부터 전체 구조를 설계하지 않고, 채널·프로그램·타깃·시청률·목표 시청률 등 당장 필요한 최소 테이블만 우선 만들고 이후 필요할 때마다 확장한다. skyUHD·2025년 연간 데이터는 메인 6개 채널과 별도 테이블로 분리하지 않고, 같은 시청률 테이블에 "데이터 종류"(예: `source_type`: nielsen_daily / skyuhd / annual_2025) 구분 컬럼을 두어 함께 관리한다.
- 고정 이유: Day 1 실습에서 쓴 것과 같은 구성이고, 3일차에 Vercel로 배포할 예정이라 그대로 이어지게 하기 위함

---

## 9. 예외 상황 처리

- Nielsen 이메일이 오지 않거나 지연되는 경우 (공휴일, 발송 시스템 장애 등): 관리자가 Excel 파일을 직접 업로드할 수 있도록 설계한다.
- Channel/Program Master에 아직 등록되지 않은 신규·개편 채널/프로그램이 Nielsen 데이터에 나타나는 경우: 매칭 실패 시 에러 메시지를 표시한다.
- PD가 자연어로 질문했는데 DB에 해당 데이터가 없는 경우:
  - 띄어쓰기만 다른 경우는 같은 프로그램으로 인식한다.
  - 회차나 `<본>`(본방송) 표시만 다른 경우도 같은 프로그램으로 인식해 통합 집계한다. 단, "본방송만" 질문하면 `<본>` 표시가 있는 회차만 필터링해 답한다.
  - 그 외에 끝내 찾지 못하는 경우, "자료 없음"이라고만 답하지 않고 **구체적으로 어떤 자료가 없는지**(예: "2026년 3월 14일 데이터가 없습니다", "ENA Story의 경쟁채널 데이터가 아직 등록되지 않았습니다") 명기한다.
- 원인 추적·기회 탐지가 트리거됐는데 경쟁채널 데이터가 부족한 경우: 에러 메시지로 어떤 데이터가 부족한지 안내하고, 관리자가 관련 파일을 수동으로 업로드할 수 있는 경로를 함께 제시한다.
- 두 기간 비교 질문에서 기간A/기간B가 겹치거나 순서가 뒤바뀐 경우: 자동으로 이른 기간을 기간A로 정렬해 계산하며, 두 기간이 겹치면 겹치는 구간을 안내하고 계산을 진행할지 확인한다.
- 신규 채널이라 프라임타임을 산출할 데이터가 아직 부족한 경우: "데이터 부족으로 프라임타임 미산출"로 표시하고, 데이터가 쌓이기 전까지는 임시 기본값(19~23시)을 사용한다.
- 목표 시청률이 아직 업로드되지 않은 기간에 대해 목표 대비 분석을 요청받는 경우: 목표 시청률은 별도의 Excel 파일로 관리자만 업로드할 수 있도록 설계한다.
- 여러 PD가 동시에 접속하거나 특정 시간대(예: 출근 직후 9시)에 접속이 몰리는 경우: 접속이 어려운 이유를 설명하는 에러 메시지를 표시한다.
- Claude가 계산에 필요한 데이터를 조회했는데 값이 깨져 있거나 형식이 맞지 않는 경우: 해당 항목은 계산에서 제외하고 별도로 오류 목록을 표시한다.
- 하루치 데이터/질문 범위가 너무 커서 한 번에 계산을 끝내지 못하는 경우: 기간을 나눠 단계적으로 계산하거나 처리 중임을 안내한다.
- PD가 질문한 시점에 그날 데이터가 아직 파이프라인 처리 중이라 DB에 반영되지 않은 경우: 최신 반영 시점을 함께 안내한다.
- 동일한 질문을 반복했을 때 결과가 달라지지 않도록, 항상 동일한 고정 공식을 적용해 재현 가능한 결과를 제공한다.
- 목표 시청률 Excel 업로드 시 채널명이 Channel Master와 일치하지 않는 경우: 업로드 시점에 검증해 불일치 시 업로드를 거부하고 오류 메시지를 표시한다.

---

## 10. 개발 단위

*Competitor Master·목표 시청률·Channel Master 속성값(시장구분/Primary Target)·주요 콘텐츠 목록은 `채널기본정보.xlsx`로 확보 완료 (위 5번 주요 기능에 반영됨). 채널 로고는 `채널 로고/` 폴더에 확보됨.*

1. Next.js + Supabase 프로젝트 연동 확인 (.env 연결 테스트)
2. DB 스키마 설계 및 생성 (채널·프로그램·타깃·시청률·목표 시청률 등 최소 테이블만 우선 생성, 이후 필요 시 확장)
3. 관리자 로그인 + 공유 링크 기반 PD 접근 제어 기능 구현 (관리자 2명, 그 외 인원은 고정 링크 하나를 공유해 열람·질문만 가능, 관리자가 링크 재발급 가능)
4. Channel Master(7개 채널·시장구분·Primary Target·프라임타임) / Competitor Master(채널별 경쟁채널 목록) 업로드 기능 구현
5. 주요 콘텐츠 관리 기능 구현 (킬러 콘텐츠 자동 산출 + 관리자 지정 주요 콘텐츠·카테고리·본방송 시간)
6. DATA_DICTIONARY.md 작성 (Nielsen 10개 시트, skyUHD `26 UHD ALL` 시트, 2025년 연간 파일 구조를 모두 문서화)
7. Nielsen Daily Excel 업로드 및 파싱 기능 구현 (6개 채널, RAW → DB 적재)
8. skyUHD 업로드 및 파싱 기능 구현 (별도 파일·형식, `시:분:초:프레임` 시간 처리)
9. 2025년 연간 집계 파일 업로드 및 YoY 기준값 저장 기능 구현
10. 데이터 품질 검증 로직 구현 (파일·구조·값·완전성 체크, 오류 시 ALERT 표시)
11. 시청률 핵심 지표 계산 기능 구현 (Rating/Share/Reach/Time Spent/Time Spent Share, DoD/WoW/MoM/QoQ/YoY/YTD)
12. 목표 시청률 업로드 기능 및 목표 대비 달성률·Gap 계산 기능 구현
13. 2026년 1~8월 데이터 업로드 및 계산 결과 테스트 (2026-01-01~08-18 일별 파일 확보 완료, 03-14만 결측 — 바로 테스트 가능. 원본 데이터는 프로젝트 폴더 내 `Nielsen Data/`에서 관리하며 `.gitignore`에 등록해 커밋되지 않도록 함)
14. Page 1 종합 대시보드(Portfolio Executive View) 및 Morning Briefing 자동 생성 로직 구현 (프라임타임 자동 산출 포함)
15. Page 2 채널별 딥다이브(Channel Deep-Dive View) 화면 구현 (기간별 시청자 흐름·프로그램 효율·경쟁 대진표)
16. 타깃 분석(Affinity·Contribution), 경쟁채널 분석(Competitive Pressure·Gap), 프로그램 유형 분석 기능 구현 — Fit Score 계산의 선행 지표이므로 편성 추천보다 먼저 구현
17. 편성 추천(Fit Score 기반 KEEP/MOVE/REPLACE/STRENGTHEN/TEST) 로직 구현 — MART_PROGRAM_TARGET_SCORE/MART_SLOT_SCORE/MART_COMPETITIVE_SCORE/MART_FLOW_SCORE/MART_SCHEDULING_FIT_SCORE 등 MART 테이블 및 가중치 CONFIG 테이블 포함
18. 자연어 질문(NL2SQL, Conversational BI) 기능 구현 — Page 2 채널별 화면 내 섹션으로 배치
19. 원인 추적(Root-Cause 참고 분석)·기회 탐지(Opportunity Alert) 기능 구현 (채널 평균 대비 -10%p 이상 하락이 3일 이상 지속 시 트리거)
20. Nielsen 메일 자동 수집 연동 구현 (메일함 접근 인증 방식 확정 후)
21. Vercel 배포 및 운영 전환
