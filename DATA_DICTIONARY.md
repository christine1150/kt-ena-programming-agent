# Nielsen 원본 데이터 구조 문서 (DATA_DICTIONARY)

> 개발 단위 6번 산출물. 파싱 코드를 작성하기 전에, 실제 업로드된 샘플 파일을 열어 시트·헤더·컬럼 구조를 있는 그대로 기록한다. **여기 적힌 내용은 전부 실제 파일을 열어서 확인한 값이며, 추측이나 PRD 초안 문구를 그대로 옮긴 것이 아니다.**

| | |
|---|---|
| 확인한 샘플 | `Nielsen Data/2026/01/닐슨_채널시청률(260101).xls`, `Nielsen Data/2026/skyUHD/26 skyUHD 시청률 (0817).xlsx` |
| 범위 | 2025~2026년 닐슨(Nielsen) 정기 파일 + skyUHD 파일. **2013~2016년 과거 자료(스카이라이프 시대, TNmS 포함)는 별도 작업으로 보류** — 채널 매핑표 도착 후 별도 문서로 다룬다. |

---

## 1. Nielsen 일별/주별/월별/연간 파일

### 1.0 공통 사실

> **파싱 시 주의(실제로 겪은 문제)**: 같은 행 안에서도 컬럼마다 엑셀 원본 셀 타입이 다르다.
> "시작시간/끝시간"은 텍스트("02:00:00")로 저장되어 있지만, **"시청시간"은 엑셀 시간 형식
> (하루=1인 소수, 예: 0.05142=1시간14분3초)으로, "시청시간비율"은 엑셀 퍼센트 형식
> (예: 0.9058=90.58%)으로 저장되어 있다.** 문자열로 바꿔서 정규식으로만 파싱하면 이 두 컬럼은
> 계속 NULL이 되거나(시청시간) 100배 작게 저장된다(시청시간비율) — 실제로 이 버그가 있었고,
> 셀 값이 숫자인지 문자열인지 스스로 판별하도록 고쳐서 해결했다 (`src/lib/nielsenDaily.ts`의
> `normalizeTime`/`timeSpentToSeconds`/`parsePercentText` 참고). 새 컬럼을 파싱할 때는
> 항상 `typeof raw === "number"` 분기를 먼저 넣고, 값이 그럴듯한지(0~100 범위 등) 실제 값으로
> 확인한 뒤에 넘어갈 것.

- 파일명 패턴: `닐슨_채널시청률(YYMMDD).xls` (하루), `닐슨_채널시청률(YYMMDD-YYMMDD).xls` (주간/월간/연간 — **시작일-종료일 범위만 다를 뿐 시트 구조는 완전히 동일**).
  - 확인됨: `Nielsen Data/2025/닐슨_채널시청률(250101-251231).xls` = 2025년 연간 파일도 이 형식 그대로.
- 한 파일에 **시트 10개 고정**:
  1. `유료방송가입가구`
  2. `개인`
  3. `ENA경쟁채널시청률`
  4. `ENA DRAMA경쟁채널시청률`
  5. `ENA PLAY경쟁채널시청률`
  6. `ONCE,OLIFE경쟁채널시청률`
  7. `ENA타깃상세`
  8. `ENA DRAMA타깃상세`
  9. `ENA PLAY타깃상세`
  10. `ONCE,OLIFE,CHING,ENA SPORTS타깃상세`
- ~~ENA Story는 이 10개 시트 어디에도 프로그램 단위 데이터가 없다~~ **정정(2026-08-19 전체 백필 중 확인): 2026년 1~2월 파일에는 없었지만, 3월부터는 있다.** 자사+타사 채널이 섞인 마지막 시트에 `ENA STORY` 섹션이 새로 추가됨 (§1.3 참고). 1~2월은 여전히 채널 단위 순위/시청률만 있고 프로그램 단위 데이터가 없다.
- 모든 시트가 **1행 헤더가 아니라, 여러 행의 안내문 + 다단(가로 반복) 헤더** 구조다. 표 형태로 만들려면 헤더가 몇 번째 행에서 시작하는지 시트 종류별로 다르게 처리해야 한다.
- 숫자 컬럼에 값이 `0.00000`으로 찍힌 경우와 완전히 빈 칸인 경우가 둘 다 존재한다 — **`0`과 `빈 칸(데이터 없음)`은 다른 의미이므로 파싱 시 절대 같은 것으로 취급하지 않는다** (CLAUDE.md NULL≠0 원칙).

### 1.1 `유료방송가입가구` / `개인` 시트 — 전체 채널 랭킹

**용도**: 우리 채널뿐 아니라 **시장 전체 채널의 시청률 순위**. 목표 대비 분석의 "목표 등위" 비교, Competitive Pressure(동시간대 상위 3개 경쟁채널) 계산의 원천 데이터.

- 3행: 분석기간 (예: `- 분석기간 : 2026. 01. 01. (목요일)`)
- 4행: 분석지역 (`유료방송가입가구`=수도권+National, `개인`=수도권만 이 샘플 기준)
- 5행: 분석대상(타깃) 목록
- **8행**: 타깃 그룹 라벨이 6컬럼씩 반복 (`- 수도권 유료방송가입가구`, `- National 유료방송가입가구`, `- 수도권 여자3049`, `- National 개인5064`  … 총 4개 블록 확인, 파일마다 타깃 조합이 다를 수 있음)
- **9행**: 실제 컬럼 헤더, 블록마다 동일하게 반복 — `No. | 채널명 | 시청률(%) | 점유율(%) | 도달율(Reach %) | 시청시간(시:분:초)` + 빈 컬럼 1개(블록 구분용)
- **10행부터 데이터** — 1위부터 순서대로 그 타깃 기준 **전체 채널** 랭킹 (지상파·종편·tvN·ENA 등 208개 채널 중 상위 다수, 샘플 파일 기준 최소 100위 이상까지 나열됨)
- 한 블록 폭 = 7컬럼(6개 데이터 컬럼 + 구분용 빈 컬럼 1개). 블록 개수만큼 좌→우로 반복.

> **같은 타깃인데 시트마다 표기가 다르다 — 실제로 겪은 문제 (Page 1 대시보드 만들며 발견).**
> ENA의 "수도권 개인 20~49세" 타깃 하나가 문서/시트에 따라 이렇게 3가지로 다르게 적혀 있다:
> - `채널기본정보.xlsx`(Channel Master의 KPI 표기): `수도권 개인2049`
> - §1.3 `OOO타깃상세` 시트(프로그램 단위): `수도권 2049` ("개인"이 빠짐)
> - 이 절(§1.1)의 `개인` 랭킹 시트: `개인2049` ("수도권"이 빠짐 — 이 시트 자체가 "분석지역: 수도권"으로 이미 한정되어 있어서 라벨에 반복 안 함)
>
> **순위(등수)는 오직 이 §1.1 랭킹 시트에만 있다** — 타깃상세 시트의 "하루전체" 집계 행에는 시청률은 있어도 순위는 없다. 그래서 "오늘 몇 위인가"를 구하려면 Channel Master 표기가 아니라 **이 랭킹 시트의 표기(`개인2049`)로 다시 찾아야 한다.** `src/app/api/dashboard/page1/route.ts`에서 여러 후보 표기를 순서대로 시도하도록 처리했다.
>
> **알려진 한계 — YoY(전년 대비) 대체값이 일부 채널엔 안 붙는다.** 2025년 연간 파일(개발 단위 9번)은 §1.1 랭킹 시트 표기(`개인2049` 등)로만 저장돼 있다. ENA/ENA Drama/ENA Play의 Page 2 트렌드는 §1.3 타깃상세 표기(`수도권 2049`)를 쓰는데, 이 이름으로는 2025년 연간 데이터를 못 찾아서 YoY 칸이 빈 채로 나온다(OLIFE/ONCE/ENA Story는 두 표기가 같아서 문제없이 나옴). 임의로 다른 값을 갖다 붙이지 않고 정직하게 "—"로 비워뒀다 — 필요하면 나중에 랭킹 시트 표기까지 보조 후보로 추가하면 해결된다.
>
> **skyUHD도 이 시트에 매일 나온다 — 오랫동안 놓치고 있던 부분(2026-08-19 Page 1 재설계 중 발견).** skyUHD는 프로그램 단위 데이터만 별도 수기 파일(§2)로 받는다고 여겨왔는데, 실제로는 `유료방송가입가구` 랭킹 시트에 "SkyUHD"라는 이름으로 다른 채널들과 나란히 채널 단위 시청률·순위가 매일 들어있다(예: 8/18 파일에서 여러 타깃 블록에 순위 167~212위로 등장). `OUR_CHANNEL_DISPLAY_NAMES`(`src/lib/nielsenDaily.ts`)에 이름이 없어서 지금까지 조용히 걸러지고 있었다 — 추가하고 `scripts/backfill-skyuhd-channel-rank.mjs`로 230일 전체를 재백필했다. skyUHD의 프로그램 단위 데이터(§2, `source_type='skyuhd'`)와는 `source_type`이 달라 서로 겹치지 않는다.

### 1.2 `OOO경쟁채널시청률` 시트 (ENA / ENA DRAMA / ENA PLAY / ONCE,OLIFE) — 자사 프로그램별 상세

**용도**: 우리 채널의 **프로그램 단위(시간대별) 시청률** 원천 데이터. `ratings` 테이블의 `program_id`·`start_time`·`end_time`이 여기서 나온다.

- 1행: 시트 제목 (`ENA 경쟁채널 시청률`)
- 2행: 날짜
- 3행: 사용된 타깃·지역 기준 각주 (예: `■ 개인2049(수도권) / 개인2039(수도권) / 유료방송가입가구(National)-KBS1, MBC, SBS는 수도권 기준`) — **타깃 조합이 파일마다 달라질 수 있으므로 매번 이 각주를 읽어서 컬럼 의미를 확인해야 한다.**
- 4행: 채널명이 **5행×2열의 블록 그리드**로 배치되어 있다 — 처음엔 "채널명 2개가 좌우로 배치"(페어링된 경쟁채널 1개)로만 봤는데, 실제로 시트 전체를 훑어보니 그 아래로 4개 행 그룹이 더 있고 각 그룹도 좌우 2개 채널 블록을 갖는 구조였다(**설계 결정 정정, 2026-08-19**). 첫 번째 행 그룹의 좌측은 항상 자사 채널 자신이고, 나머지 8~9개 블록이 전부 그 채널의 실제 경쟁채널이다(날짜 무관 고정, 2026-03-01/08-18로 재확인):
  - `ENA경쟁채널시청률`: **ENA**(자사) / tvN / SBS / MBC / KBS2 / JTBC / KBS1 / 채널A / TV CHOSUN / MBN
  - `ENA DRAMA경쟁채널시청률`: **ENA DRAMA**(자사) / ENA STORY(자사 채널이라 skip) / SBS Plus / MBC드라마넷 / KBS드라마 / tvN STORY / tvN DRAMA / JTBC2 / DRAMAcube / Dramax
  - `ENA PLAY경쟁채널시청률`: **ENA PLAY**(자사) / MBC every1 / SBS funE / KBS JOY / 채널S / tvN SHOW / Mnet / E채널 / 코미디TV / theLIFE
  - `ONCE,OLIFE경쟁채널시청률`: **ONCE**(자사) / **OLIFE**(자사, skip) / EDGETV / CNTV / D-ONE / MBC ON / K STAR / CH view
  - 위 채널명은 Competitor Master(`competitors` 테이블)에 등록된 이름과 정확히 일치함을 실데이터로 확인했다(OLIFE는 등록된 경쟁채널 CMCTV가 이 시트에 없어 프로그램 단위 데이터를 못 얻음 — "자료 없는 채널은 제외").
- 5행: `시작시간 | 끝시간 | 프로그램명 | 시청률(3개 타깃 병합헤더) | 점유율(3개 타깃 병합헤더)` (블록마다 반복)
- 6행: 5행의 "시청률"/"점유율" 아래 실제 타깃명 3개씩 (예: `개인2049 | 개인2039 | 유료방송가구`)
- **7행부터 데이터**, 각 블록은 그 채널의 "하루 전체" 요약 행에서 끝난다(블록마다 데이터 행 수가 다름 — 채널마다 그날 프로그램 개수가 다르므로)
- 컬럼 폭: 블록 하나당 9컬럼(시작/끝/프로그램명 + 시청률×3 + 점유율×3), 블록 사이 구분 빈칸 1개

> **설계 결정 변경(2026-08-19, 실데이터 재조사로 확정)**: 처음엔(개발 단위 7번) 이 시트를 아예 안 읽기로 했다가, 이후 "우측 블록 1개만 페어링된 경쟁채널"로 오판해 R4 헤더 채널명(예: "tvN") 하나만 읽고 시트 끝까지 그 이름으로 저장했었다. 그 결과 "tvN" 헤더인데 "MBC뉴스데스크"·"JTBC뉴스룸" 같은 명백히 다른 채널의 프로그램명이 섞여 나오는 문제가 있었고, 한때는 이걸 "산출 기준 불명 데이터"로 보고 화면 노출을 보류했었다. **직접 파일을 셀 단위로 열어 재조사한 결과, 이건 데이터 문제가 아니라 파서가 블록 경계(각 채널의 "하루 전체" 종료 행)를 무시하고 시트 끝까지 읽어버린 버그였다** — 실제로는 5행×2열 그리드에 8~9개 별개 채널 블록이 있고, 각 블록은 자기 채널의 프로그램만 정확히 담고 있다. `findCompetitorBlocks()`로 "시작시간" 헤더 셀을 전부 찾아 블록별로 자기 "하루 전체" 행까지만 읽도록 고쳤다(`src/lib/nielsenDaily.ts`). `competitor_program_ratings`(유니크 제약에 `competitor_name` 추가)로 저장하며, 2026-01-01~08-18 전체 230개 파일을 재백필했다(147,458건, `scripts/backfill-competitor-program-ratings.mjs`). Page 2 COMPARED WITH?의 `get_competitor_program_overlap`/`get_competitor_top_programs`, Page 1 Original 리포트의 경쟁 프로그램 비교(§7-1 참고)가 이 데이터를 쓴다.

### 1.3 `OOO타깃상세` 시트 (ENA / ENA DRAMA / ENA PLAY / ONCE,OLIFE,CHING,ENA SPORTS) — 자사 프로그램 × 다중 타깃

**용도**: Fit Score의 Target Performance·Target Affinity·Audience Engagement(Reach·Time Spent·Time Spent Share) 계산 원천.

- 4행: `■ 시청시간/시청시간비율 : 본 사람 기준 (hh:mm:ss)` 안내
- 5행: `■ 점유율(%) : 지상파 + 종편 + 2026년 기준 유료방송 208개 채널` 안내 — **연도마다 분모 채널 수가 달라질 수 있음을 시사(2026년 기준 208개). 연도별 파일마다 이 문구를 확인해야 한다.**
- 7행: 채널명 + 타깃 라벨 (`수도권 2049`, `수도권 2039`, `전국 유료가구`, `수도권 남0409` …) — 샘플에서 **타깃 블록이 88컬럼 ÷ 5컬럼 ≈ 17개**까지 반복됨 (일별 파일마다 타깃 개수가 다를 수 있음)
- 8행: 각 타깃 블록의 실제 컬럼 헤더 5개 — `시청률 | 점유율 | 도달율 | 시청시간 | 시청시간비율`
- 공통 컬럼(맨 앞 3개, 블록 반복에 포함 안 됨): `시작시간 | 끝시간 | 프로그램명`
- `ONCE,OLIFE,CHING,ENA SPORTS타깃상세` 시트처럼 **여러 채널이 한 시트에 같이 들어있는 경우** — 확인 완료: 행이 위아래로 쌓이는 구조다. 채널명이 A열에 단독으로 나오는 행(예: `ONCE`) 다음 행이 `시작시간`으로 시작하면 그게 새 채널 섹션의 시작이고, 그 채널의 데이터가 끝나면(하루전체 행) 빈 행 몇 개를 건너뛰고 다음 채널 섹션이 이어진다. 이 조합 시트에서는 채널마다 타깃 블록 개수가 다를 수 있다(ONCE/OLIFE는 1개, ENA 전용 시트는 17개).
- **각 채널 섹션의 마지막 행은 "하루전체"(그날 전체 집계)인데, 다른 데이터 행과 열 배치가 다르다 — 실제로 겪은 문제.** 일반 데이터 행은 `시작시간(A) | 끝시간(B) | 프로그램명(C) | 타깃블록...(D~)` 순서지만, 하루전체 행은 `"하루전체"(A) | 빈칸(B) | 빈칸(C) | 타깃블록...(D~)`로 **"하루전체" 글자가 프로그램명 칸이 아니라 시작시간 칸에 있다** (타깃블록 값 위치 자체는 동일). 프로그램명 칸(C열)만 보고 "하루전체"를 찾으면 이 행을 통째로 놓친다 — A열도 함께 확인해야 한다 (`src/lib/nielsenDaily.ts` 참고).
- **이 조합 시트의 "이름" 자체가 중간에 바뀐 적이 있다 — 실제로 겪은 문제.** 2026년 1~2월은 `ONCE,OLIFE,CHING,ENA SPORTS타깃상세`였는데, 3월부터 `ONCE,OLIFE,ENA SPORTS타깃상세`로 (CHING이 빠지고) 바뀌었다 — 그리고 이때부터 `ENA STORY` 섹션이 새로 추가됐다. 정확한 시트 이름을 하드코딩하면 3월 이후 파일이 전부 "필수 시트를 찾을 수 없음"으로 실패한다 — 실제로 이 문제로 8개월치 백필 중 171개 파일이 한 번에 실패했었다. `/^ONCE,\s*OLIFE.*타깃상세$/` 같은 패턴으로 찾도록 고쳤다.

---

## 2. skyUHD 파일

파일명 패턴: `26 skyUHD 시청률 (MMDD).xlsx` (그때그때 최신 날짜까지 누적, 수기 업데이트).

> **파싱 시 주의(실제로 겪은 문제)**: `날짜` 컬럼은 엑셀에 텍스트가 아니라 **날짜 형식(숫자 직렬값)**으로 저장되어 있다. 이 값을 문자열로 바꿀 때 JS `Date` 객체의 로컬/UTC 타임존에 의존하면 서버 위치에 따라 하루가 밀릴 수 있어(실제로 하루 밀리는 버그가 났었음), 정수 연산으로 직접 변환해야 한다 (`src/lib/skyUhd.ts`의 `excelSerialToIsoDate` 참고).

### 2.1 `26 UHD ALL` 시트 — ✅ 파싱에 사용할 메인 시트

가장 단순하고 이미 정규화된 표 형태:

| 컬럼 | 예시 값 | 비고 |
|---|---|---|
| 날짜 | `2026-01-01` | |
| 요일 | `목` | |
| 시작시간 | `13:36:53:42` | **시:분:초:프레임** — DB의 `time` 타입에 넣으려면 프레임을 버리거나 초 단위로 환산해야 함 |
| 끝시간 | `14:21:24:44` | 위와 동일 |
| 프로그램명 | `대문 밖은 사파리 1회` | 회차가 이름에 포함되어 있음 → `episode_number` 분리 필요 |
| 시청률 | `0.03430` | 단일 값 (타깃 구분 없음 — skyUHD는 전체가구 기준으로 추정, 확인 필요) |
| 비고 | (대부분 빈칸) | |

- 1행이 헤더, 2행부터 바로 데이터 — **다단 헤더 없이 일반적인 표** (10개 시트짜리 Nielsen 파일과 가장 다른 점).
- 1460행까지 있음 (2026년 1~8월 누적으로 추정, 날짜순 정렬이 아니라 시청률 순 정렬로 보임 — 실제로 R2~R10에서 날짜가 뒤섞여 있고 시청률이 높은 순으로 보임. **정렬 기준을 코드에서 가정하지 말고 `날짜` 컬럼으로 다시 정렬해서 처리해야 한다.**)

### 2.2 `skyUHD 1월`~`8월` 시트 — 참고용, 파싱 비대상

`26 UHD ALL`과 같은 원천 데이터를 "하루 단위로 옆으로 나열"한 사람이 보기 편한 뷰로 추정된다 (요일별 3~4일씩 옆으로 붙는 다단 구조, 위쪽에 순위 번호와 총 시청률 요약이 따로 있음). **`26 UHD ALL` 시트 하나로 충분히 같은 정보를 얻을 수 있어, 이 8개 시트는 파싱하지 않는다** (같은 데이터를 두 번 파싱해 불일치가 생기는 것을 피함).

---

## 3. 2025년 연간 파일 (`닐슨_채널시청률(250101-251231).xls`) — 확인 완료

실제로 열어본 결과, **일별 파일(§1)과 다른 부분이 있다**:

- 시트가 10개가 아니라 **`유료방송가입가구`, `개인` 2개뿐**이다. `OOO경쟁채널시청률`·`OOO타깃상세`(프로그램 단위) 시트는 아예 없다.
- `유료방송가입가구`/`개인` 시트 구조 자체는 §1.1과 완전히 동일 (다단 헤더, 7컬럼 블록, No./채널명/시청률/점유율/도달율/시청시간). 3행 "분석기간"만 `2025.01.01 - 2025.12.31`처럼 하루가 아니라 연 전체 범위로 되어 있고, 그 범위 전체에 대한 **연간 평균값 한 줄**이 채널당 있다 (365일치가 따로 들어있는 게 아니다).
- 즉 이 파일로는 **채널 단위 연간 평균 순위·시청률만** 얻을 수 있고, 프로그램 단위 데이터나 일별 YoY 비교(예: "1월 15일 vs 작년 1월 15일")는 이 파일만으로 불가능하다. "YoY 기준값"은 연간 평균 대 연간 평균 비교 수준으로 이해해야 한다.
- `ratings.source_type = 'annual_2025'`를 그대로 사용하며, `broadcast_date`는 그 해 마지막 날짜(`2025-12-31`)로 저장하는 규칙을 정했다 (연간 값이라 특정 날짜가 없어서, "그 해 기준"이라는 의미로 12월 31일을 쓴다). `program_id`는 항상 null.

---

## 4. 보류된 범위 — 2013~2016년 과거 자료

`Nielsen Data/기타 이전 자료/` 폴더(2013년 4월~2016년 12월, 약 11,151개 파일)는 사용자 요청으로 **오늘은 다루지 않는다**. 확인된 사실만 기록해둔다 (파싱 설계는 채널 매핑표 도착 후 별도 문서에서 진행):

- 연도별로 파일 포맷과 조사기관이 여러 번 바뀜 (2013: 종편 비교 + 자사채널 다단 시트, 2014: 닐슨_IPTV/스카이라이프, 2015: SKYTV/TNmS, 2016: TNMS 월보/일보/주보)
- **TNmS 자료는 이번 범위에서 제외** (사용자 지시 — Nielsen 자료만 사용)
- 옛 채널명 매핑 확인된 것: `sky` 채널 → **ENA**, `채널T` → **OLIFE**. 나머지는 매핑표 도착 후 확정.

---

## 5. 경쟁채널 시청률 저장(`competitor_ratings`) 및 Competitive Pressure·Affinity 계산 (개발 단위 16번)

**출처**: §1.1의 `유료방송가입가구`/`개인` 전체 랭킹 시트를 `parseRankSheet()`로 두 번 파싱한다 — 1차는 우리 6개 채널 이름으로(기존과 동일), 2차는 Competitor Master(`competitors` 테이블)에 등록된 채널명으로. `get_competitive_pressure()`/`get_target_affinity()`는 이 채널 단위 랭킹 데이터(최근 7일 평균 근사)를 쓴다 — "동시간대에 경쟁채널이 무엇으로 잘했는가" 같은 프로그램 단위 인사이트는 §1.2(`OOO경쟁채널시청률`)에서 나온 `competitor_program_ratings`를 쓴다(§1.2 참고, 예전엔 "채널당 경쟁채널 1개만 제공"으로 잘못 파악해 안 썼었지만 실제로는 8~9개 전체 채널을 제공하는 것으로 정정됨).

**타깃 라벨 종류가 제한적이다 — 실제로 겪은 문제**: 랭킹 시트에 있는 타깃 블록만큼만 경쟁채널 데이터가 생기므로, `competitor_ratings`에는 8종류 라벨만 존재한다(`National 개인5064`, `National 유료방송가입가구`, `개인2039`, `개인2049`, `개인3549`, `수도권 여자3049`, `수도권 유료방송가입가구`, `여자3039`). 반면 우리 채널 자신의 "매칭 타깃 라벨"(`get_target_achievement`가 찾아주는 라벨)은 채널마다 다른 시트에서 나와 표기가 다르다:

- ENA·ENA Drama·ENA Play: 자사 매칭 라벨 `수도권 2049` ↔ `competitor_ratings` 라벨 `개인2049` (다른 표기, 같은 타깃)
- OLIFE·ONCE·ENA Story: 자사 매칭 라벨 `National 유료방송가입가구` ↔ `competitor_ratings` 라벨도 동일 `National 유료방송가입가구` (문제 없음)

`get_competitive_pressure()` SQL 함수는 정확히 일치하는 라벨로 먼저 찾고, 경쟁채널 데이터가 0건이면 "수도권/National 접두어 제거 + 개인 접두" 규칙으로 한 번 더 시도한다 (`supabase/migrations/20260819120000_fix_competitive_pressure.sql`).

**Postgres `LEAST(100, NULL)` 함정 — 실제로 겪은 문제**: Postgres의 `LEAST`/`GREATEST`는 인자 중 NULL이 아닌 값들 중에서 고른다(둘 다 NULL일 때만 NULL). 그래서 `LEAST(100, NULL)`은 `NULL`이 아니라 **100을 반환**한다 — 경쟁채널 데이터가 전혀 없을 때 Competitive Pressure를 "100(최대 압박)"으로 잘못 표시하는 버그로 이어졌었다(NULL≠0 원칙 위반). `CASE WHEN ... IS NULL THEN NULL ELSE LEAST(...)` 형태로 명시적 분기해야 한다.

**Affinity의 지역 스코프 불일치 — 실제로 겪은 문제**: WHO IS WATCHING?의 연령대별(20대/40대 남녀) 데이터가 채널의 시장 스코프(`channels.market`: `수도권` vs `전국`)에 따라 다른 라벨로만 존재한다 — 예: OLIFE/ONCE/ENA Story는 `전국 여20대`는 있지만 `수도권 여20대`는 없다(실데이터 230일 전체로 확인). 비교 대상 채널과 타깃 라벨을 둘 다 채널의 시장 스코프에 맞춰 골라야 한다: 수도권 채널(ENA·ENA Drama·ENA Play)은 서로 비교, 전국 채널(OLIFE·ONCE·ENA Story)도 서로 비교(`src/app/api/dashboard/channel/route.ts`의 `isNationalScope` 분기).

---

## 6. 편성 추천(Fit Score) MART 계산 설계 판단 (개발 단위 17번)

PRD.md 5번의 Fit Score 공식(30/20/15/15/10/10 가중치, 각 하위지표는 최근 12주 percentile)은 몇몇 세부사항을 정확히 정의하지 않아, 아래는 실제 구현하면서 정한 판단이다 — 임의 추측이 아니라 원본 데이터 구조상 불가피했던 선택이며, 정직하게 문서화해둔다(개발 단위 16번의 Competitive Pressure "기간 평균 근사"와 같은 성격).

- **Program의 단위**: `programs.canonical_name`(channel_id+canonical_name으로 회차를 넘어 이미 하나의 id로 통합돼 있음, 관리자 업로드 로직의 `onConflict: "channel_id,canonical_name"` 참고) — 회차별이 아니라 시리즈 단위로 Fit Score를 낸다.
- **Daypart 4구간**: 새벽(02~08시)/오전(09~13시)/오후(14~18시)/저녁·심야(19~25시). PRD에 정확한 경계가 없어 방송 편성 관행에 따라 정했다.
- **Target Affinity·Competitive Opportunity는 프로그램별이 아니라 채널 단위로 계산**해 그 채널의 모든 프로그램에 동일하게 적용한다 — 원본 자료가 프로그램별 세부 타깃 데이터(경쟁채널 전체)나 경쟁채널의 프로그램별 시간대 데이터를 충분히 제공하지 않기 때문. Target Affinity는 WHO IS WATCHING?과 같은 대표 연령대 4개(수도권/전국 스코프에 맞춰 선택) Affinity Index 평균을 6개 채널간 percentile로 환산한 값을 쓴다 — **실제로 겪은 문제**: 이 percentile 계산에서 Affinity 데이터가 아예 없는 채널(전부 표본 부족)까지 `percent_rank()`에 포함시키면 Postgres가 NULL을 정렬 맨 뒤로 보내 "데이터 없음"이 "가장 좋음"(100)으로 둔갑한다 — NULL인 채널은 percentile 계산에서 아예 제외해야 한다.
- **개별 percentile 하위지표에 쓸 데이터가 없으면 50(중앙값)으로 처리**해 전체 계산이 막히지 않게 한다(0으로 임의 대체하지 않음). 6개 상위 지표 중 Audience Flow만 정말 값이 없을 수 있는데(그 프로그램이 항상 그날 첫 방송이라 이전 프로그램이 없는 경우), 이때는 Audience Flow를 빼고 나머지 5개 가중치로 재정규화한다.
- **Confidence(%) = min(100, 표본일수 ÷ `fit_score_config.full_confidence_sample_days`(기본 16) × 100)**. Confidence가 `min_confidence_pct_for_tag`(기본 50%) 미만이면 Fit Score와 무관하게 태그를 TEST로 표시한다(PRD.md 예시 "Confidence 38%(표본 6건)"와 동일한 계산식으로 실데이터에서도 재현됨).
- **실제로 겪은 SQL 버그 3건**(전부 실데이터 재검증 중 발견·수정): (1) `round(double precision, int)`은 Postgres에 없어 `percent_rank()`(double precision 반환) 결과에 `::numeric` 캐스팅이 필요했다. (2) `GROUP BY` 표현식을 별칭 없이 그대로 `SELECT`에 다시 쓸 수 없어(별칭은 group by 절에서만 유효) 명시적으로 `as` 별칭을 줘야 했다. (3) `mart_competitive_score.competitive_opportunity_score`를 채우는 UPDATE문을 함수 맨 끝에 뒀더니, 그보다 먼저 실행되는 최종 결합(MART_SCHEDULING_FIT_SCORE 생성) 단계가 아직 채워지지 않은 NULL을 읽어 모든 프로그램의 Competitive Opportunity가 중립값 50으로 통일되는 버그가 났다 — 계산 순서(먼저 채우고 나중에 join)를 지켜야 한다.

---

## 7. 원인 추적(Root-Cause)·기회 탐지(Opportunity Alert) 설계 판단 (개발 단위 19번)

PRD.md 5번의 "동시간대 경쟁채널의 편성 변화"를 감지하는 규칙(①주요 프로그램 교체 ②편성 시간·프로그램 변경 ③신규 편성 ④기존 편성 이탈)은 **경쟁채널의 프로그램 단위 시간대별 데이터**가 있어야 판별할 수 있다. 이 기능(개발 단위 19번) 작성 시점에는 그 데이터를 저장하지 않기로 했었으나, 이후(§1.2 참고) `competitor_program_ratings`로 등록 경쟁채널 전체의 프로그램 단위 하루 편성을 저장하게 됐다 — 다만 "편성이 어제와 오늘 사이 바뀌었는가"를 날짜 간 비교하는 로직은 이 문서 작성 시점에는 아직 만들지 않았고, 아래 채널 단위 근사만 구현돼 있다(향후 이 데이터로 재구현 가능).

그래서 "편성이 바뀌었다"고 프로그램명 단위로 단정하지 않고, **경쟁채널의 채널 단위 일별 시청률이 전주 대비(원인 추적) 또는 최근 7일 대비 이전 7일(기회 탐지) 5%p/10%p 이상 움직였는지**만 참고 정보로 제공한다 — "편성 변화"가 아니라 "경쟁채널 시청률 변동"으로 화면·API 문구 모두에 정직하게 표현했다. 상관관계일 뿐 인과관계로 단정하지 않는다는 PRD 원칙(6번 비범위의 Audience Flow 추적 금지와 같은 취지)도 그대로 지켰다.

기준 자체는 PRD 고정값을 그대로 썼다(원인 추적: 최근 28일 평균 대비 -10%p 이상 하락이 3일 연속, 기회 탐지: 최근 7일 대비 이전 7일 ±10%p). "채널 평균"의 기준 기간(28일)과 기회 탐지의 비교 윈도우(7일/7일)는 PRD가 정확히 명시하지 않아, WHY?/OPPORTUNITY?의 다른 계산(Affinity 28일, Competitive Pressure 7일)과 일관되게 정했다. 실데이터 검증: 2026-08-18 기준 OLIFE가 실제로 3일 연속 하락 트리거(3일 각각 -16.4%/-10.0%/-13.2%, 전부 -10%p 이하)됐고 CMCTV가 전주 대비 +37.0% 변동한 것을 확인했으며, 2026년 5~8월 구간을 스캔해 ONCE·ENA Story 등에서 기회 탐지가 실제로 여러 차례 트리거되는 것도 확인했다(항상 false만 나오지 않음을 검증).

---

## 8. Original 리포트 화이트리스트 (`original_review_programs`) 설계 판단

Page 1 Original 리포트는 그날 방영된 프로그램을 임의로 골라잡지 않고, 관리자가 `채널기본정보.xlsx`의 "요일 별 리뷰 프로그램" 시트에 지정한 요일별 프로그램만 분석한다(사용자 지시). 시트 열: `요일 | 프로그램명 | 본방 채널 | 본방 시간 | 비고 | 직재방 채널`.

- **"요일"은 그 요일의 첫 행에만 적히고 나머지는 빈칸**(엑셀 병합 셀처럼 보이는 구조) — 파서가 이전 값을 이어서 쓴다(`src/lib/originalReviewSchedule.ts`).
- **"본방 시간"이 두 가지 형식으로 섞여 있다**: 대부분 "밤 10시"/"오후 5시 40분" 같은 한글 텍스트지만, 자정을 넘기는 항목(예: 화요일 밤~수요일 새벽으로 넘어가는 "아이돌 파견근무")은 엑셀이 시간 형식 숫자(0.02777...=00:40)로 저장해뒀다 — `parseKoreanBroadcastTime()`이 숫자/문자열 둘 다 처리한다.
- **금요일처럼 "NULL"만 적힌 요일**은 프로그램이 없다는 뜻 — 화이트리스트에 그 요일 행을 아예 안 남긴다. "매월 넷째 주"처럼 조건부로 편성되는 프로그램(예: 화요일 "ENA 케이팝업 차트쇼")도 그 조건을 코드로 흉내 내지 않는다 — 실제 방영 데이터가 없는 주는 매칭이 실패해 자동으로 리포트에서 빠지므로, 조건 해석 로직 자체가 필요 없다.
- **"본방 시간 ±10분 이내로 시작하는 것이 본방송"**(사용자 지시)을 그대로 구현했는데, **처음엔 이 조건 하나만으로 매칭했다가 실데이터로 버그를 발견했다** — 그 시간대에 우연히 방영 중이던 **다른** 프로그램이 화이트리스트 프로그램으로 잘못 매칭됐다(TV로 방영되지 않는 "[웹예능]에나분식"이 같은 시간대 방영 중이던 "유부녀킬러"에 매칭되는 등). "±10분"은 프로그램을 찾는 기준이 아니라 "그 프로그램의 여러 방영 중 어느 게 본방송인지"를 가리는 기준이므로, 프로그램명이 먼저 일치(`regexp_replace(name, '[^0-9A-Za-z가-힣]', '', 'g')`로 공백·쉼표·괄호 등 문장부호를 무시하고 비교)해야만 시간 매칭을 적용하도록 고쳤다(`get_original_content_daily`/`get_original_content_weekly_review`).
- **자정을 넘기는 프로그램의 실제 저장 위치**: Nielsen은 02:00~다음날 25:59를 "하루"로 보므로(§1 공통 관행), 본방 시간이 02:00 이전(예: 00:40)인 화이트리스트 항목은 실제로는 **하루 전 날짜의 파일**에 저장돼 있다 — SQL 함수가 `broadcast_time < '02:00:00'`이면 조회 대상 `broadcast_date`를 하루 앞당긴다.
- **"동시방송 비교"(예: 수/목 나는 SOLO ↔ SBS Plus)는 프로그램명을 하드코딩하지 않고 일반화**했다 — 화이트리스트에 걸린 채널마다 `get_competitor_program_overlap`(§1.2 참고, Page 2 COMPARED WITH?와 동일한 함수)을 재사용해 실제 시간이 겹치는 등록 경쟁채널 프로그램(상위 3개)을 그대로 붙인다. 그 시간대에 SBS Plus가 방영 중이면 자연히 나타나고, 아니면 안 나타난다.
- 화이트리스트가 없는 요일(금요일)은 최근 7일간 화이트리스트 프로그램들의 실적을 종합하는 주간 리뷰로 자동 대체된다(`get_original_content_weekly_review`) — "오늘 없으면 이번 주를 리뷰"라는 사용자 지시를 그대로 구현.

---

## 9. Page 2(채널별 딥다이브) 재설계 — 보고서형 화면 (2026-08-20)

Page 2의 오늘의 브리핑/WHO IS WATCHING?/HOW DEEPLY?/CONTENT FITS?/OPPORTUNITY?/COMPARED WITH?를 카드 나열 대신 **줄글 보고서**로 재구성(사용자 지시)하면서 새로 만든 SQL 함수와 설계 판단.

- **`get_channel_daily_narrative`에 `dow_baseline_avg_rating` 추가**: "오늘 요일이 평소 강세/약세 요일인지" 판단하려면 같은 요일끼리만 모은 baseline 평균이 필요해서, `extract(isodow from broadcast_date) = extract(isodow from p_as_of_date)` 조건의 CTE를 추가했다. Postgres는 `create or replace function`으로 OUT 파라미터(반환 테이블 컬럼)를 바꿀 수 없어(`42P13`) `drop function` 후 재생성해야 했다. Page 1(28일 baseline)과 Page 2 오늘의 브리핑(12주=84일 baseline)이 `p_baseline_days` 인자만 다르게 같은 함수를 공유한다.
- **`get_competitor_insight_report`**: COMPARED WITH?의 Competitive Pressure 숫자를 없애고, 등록 경쟁채널을 오늘 순위 순으로 나열하며 12주 평균 대비 등락 + 오늘 최고 성적 프로그램(시간대)을 함께 준다. **처음 만들 때 자사 매칭 타깃 라벨("수도권 2049")을 그대로 `competitor_ratings` 조회에 써서 0건만 나온 버그가 있었다** — `competitor_ratings`는 §1.1 랭킹 시트 표기("개인2049")를 쓴다는, `get_competitive_pressure`에서 이미 겪고 고친 문제(§5 참고)를 새 함수에 반영하지 않아 재발한 것. 같은 동의어 폴백(`language sql` → `language plpgsql`로 전환해 `수도권/National` 접두어를 떼고 "개인"을 붙인 라벨로 재시도)을 옮겨와 고쳤다(`20260820010000_competitor_insight_label_fallback.sql`).
- **`get_channel_daypart_opportunity`**: daypart(새벽/오전/오후/저녁·심야, Fit Score MART와 동일한 4구간)별로 "우리 vs 등록 경쟁채널" 격차가 보유 기간 전체(기본 365일) 대비 최근 1주 사이 어떻게 바뀌었는지 계산한다. `gap = 경쟁채널 평균 − 우리 평균`, `gap_change = gap_full − gap_recent` (양수면 최근 들어 격차가 좁혀짐=경쟁채널이 상대적으로 약해짐=기회). **리뷰 중 발견한 설계 개선점**: 처음 버전은 "최근 1주" 구간이 "전체 365일" 평균 계산에도 그대로 포함돼 있어(다른 함수들처럼 baseline에서 최근 구간을 제외하지 않음) 최근 데이터가 자기 자신과 비교되며 격차를 옅게 만드는 미세한 편향이 있었다 — `get_channel_daily_narrative`의 baseline이 항상 `p_as_of_date - 1`까지만 쓰는 관행과 다르길래, `full_avg` 계산에서도 최근 구간(`broadcast_date <= p_as_of_date - p_recent_days`)을 제외해 "직전 기간 평균 vs 최근 1주 평균"이 되도록 바로잡았다(`20260820020000_daypart_opportunity_exclude_recent.sql`).
- **`get_hourly_program_titles`**: 02~26시 막대그래프에 시간대별 실제 방영 프로그램명을 붙이기 위한 함수. 사용자가 처음에는 "선형 그래프로"라고 했다가 같은 메시지 끝에서 "막대 형태는 유지하고 프로그램명만 보이게 하자"로 정정해, 막대 형태를 유지하고 막대 tooltip + 그래프 하단 "시간: 프로그램명" 목록 두 군데에 반영했다.
- **우측 상단 기간 설정(DoD/WoW/MoM/QoQ/YTD/직접 선택)**: 클라이언트에서 프리셋 날짜를 계산할 때 `new Date(`${latest}T00:00:00`)`(로컬 시각 파싱) 후 `.toISOString()`(UTC 변환)을 섞어 써서, 브라우저 로컬 타임존이 UTC+인 경우 자정 근처에서 날짜가 하루 당겨지는 버그가 실제로 있었다 — `getFullYear/getMonth/getDate`만으로 문자열을 만드는 `toDateStr()`로 고쳤다(`src/app/channel/ChannelDeepDive.tsx`). **리뷰 중 추가로 발견한 잠재 버그**: MoM/QoQ가 쓰는 `Date.setMonth()`는 월말 날짜에서 다음 달로 자동 롤오버되는 JS 기본 동작이 있어(예: 5/31에서 -3개월 시 2/31이 없어 3/3로 밀림) 월말 날짜를 기준일로 볼 때 잘못된 날짜가 나올 수 있었다 — 대상 월의 마지막 날짜로 클램프하는 `addMonthsClamped()`로 교체했다(테스트 당시 8월 중순 날짜만 써봐서 이 경로는 실제로 발견되지 않았던 잠재 결함).
- **Fit Score API(`/api/scheduling/fit-score`) 동시 요청 경합**: 이전 세션에서 "동시에 여러 프리셋을 빠르게 클릭하면 간헐적 500"이 관찰만 되고 원인 미상으로 남아있었다 — `refresh_fit_score_mart()`가 `delete where as_of_date=X` 후 재삽입하는 구조라, 같은 날짜를 처음 조회하는 두 요청이 동시에 들어오면 서로의 삭제/삽입이 겹쳐 실패할 수 있는 경합 조건이었다. 재계산이 실패해도 그 사이 다른 요청이 이미 채워놨을 수 있으니, 에러를 바로 반환하기 전에 MART를 한 번 더 확인해 데이터가 있으면 정상 응답하도록 고쳤다(완전한 락은 아니지만 PD의 일반적인 단일 클릭 사용에는 충분).
- **Fit Score 콜드 계산의 진짜 원인은 역할(role) 단위 statement_timeout이었다**: 기간 설정에서 아직 계산된 적 없는 날짜(예: MoM/QoQ/직접 선택)를 고르면 브라우저에서 실제로 500("canceling statement due to statement timeout")이 재현됐다. `src/lib/supabase.ts`는 서버에서도 anon 키로 접속하는데(접근 제어는 Next.js 세션 쿠키로 앱 레이어에서 하므로 지금까지 문제가 없었음), Supabase 프로젝트의 `anon` 역할 자체에 `statement_timeout=3s`(authenticated는 8s)가 설정돼 있었다 — `refresh_fit_score_mart()`는 채널 6개를 순회하며 여러 하위 함수를 호출하는 무거운 콜드 계산이라 직접 연결 테스트로도 편도 4~5초가 걸려 이 3초 제한에 쉽게 걸렸다. **먼저 시도한 "함수 안에서 `set_config`로 늘리기"는 효과가 없었다** — Postgres는 PostgREST가 보낸 최상위 SQL 문 자체의 타임아웃 타이머를 그 문이 시작되기 전 세션 기본값으로 이미 걸어두므로, 함수 내부에서 값을 바꿔도 이미 걸린 타이머에는 반영되지 않는다(재현 테스트로 직접 확인: 함수 내부 수정만으로는 새 날짜가 계속 500이었다가, 역할 자체의 기본값을 20초로 올린 뒤에야 성공했다). `20260820040000_anon_role_statement_timeout.sql`로 `anon`/`authenticated` 역할의 `statement_timeout`을 20초로 올려 해결 — 이 앱은 공개 미인증 트래픽이 anon 키로 직접 DB를 두드리는 구조가 아니라(모든 화면이 Next.js 세션/공유링크로 먼저 걸러짐) 역할 단위로 넉넉히 올려도 안전하다고 판단했다.

---

## 10. Page 2 기간 설정 — 단일 일자 → 기간 범위(YYYY-MM-DD~YYYY-MM-DD) 전면 확장 (2026-08-20)

사용자 지시: 우측 상단 기간 설정에서 하루뿐 아니라 기간(최근 7일~1년 이상, 직접 선택 from~to)도 고를 수 있고, 그 선택에 따라 오늘의 브리핑부터 COMPARED WITH?까지 대부분의 섹션이 실제로 재계산되도록 확장. `supabase/migrations/20260820050000_page2_period_range_functions.sql`.

- **원칙**: 기존 단일 일자 함수를 갈아엎지 않고, 하루 단위로 고정돼 있던 함수만 `p_date_from`(선택 인자)을 추가해 "기간이면 평균, 안 주면(=단일 일자) 기존과 100% 동일"하게 확장했다 — 기존 동작을 절대 깨지 않기 위한 설계 원칙.
- **`get_rating_period_report`(신규)**: 기간 평균, 직전 동일 길이 기간 대비, 그 기간 시작 이전 12주 평균 대비, 기간 중 최고/최저일을 한 번에 낸다. 기존 `get_rating_summary(date_from, date_to)`를 세 번(기간/직전기간/baseline) 재사용해 새로 계산 로직을 만들지 않았다. WHAT HAPPENED?/HOW DEEPLY?/오늘의 브리핑의 기간 범위 버전이 전부 이 함수 하나에서 나온다.
- **`get_hourly_program_titles`**: `p_date` 하나였던 걸 `p_date_from`/`p_date_to`로 바꿔 기간 전체의 프로그램명을 모을 수 있게 했다(OUT 컬럼은 그대로지만 인자 이름이 바뀌어 `drop function` 필요).
- **`get_competitor_insight_report`/`get_competitor_top_programs`**: 둘 다 `p_date_from`(기본 null)을 추가했다. **Postgres 함정**: 기존 함수에 파라미터를 추가하면 `create or replace`가 "같은 함수 교체"가 아니라 다른 시그니처의 **오버로드(별도 함수)로 취급**돼(파라미터 목록이 다르므로), PostgREST가 `get_competitor_insight_report is not unique` 에러로 함수를 특정하지 못하는 문제가 실제로 발생했다 — `drop function if exists <이전 시그니처>`를 새 `create or replace` 앞에 먼저 넣어야 한다(반환 컬럼을 추가한 `get_competitor_top_programs`도 동일 이유로 drop 필요). `p_date_from`을 주면 baseline(12주)을 **그 기간 이전으로 계산**해 겹치지 않게 했다(§9의 daypart_opportunity 이중포함 버그를 처음부터 피한 설계).
- **의도적으로 기간 확장하지 않은 것 — `get_competitor_program_overlap`("오늘 시간대별 경쟁 프로그램")**: 여러 날을 합치면 같은 시간대에 서로 다른 날짜의 프로그램이 뒤섞여 표시돼 오히려 의미가 흐려진다(동시간대 페어링 비교라는 이 섹션의 목적과 상충) — 그래서 기간을 선택해도 항상 선택 기간의 마지막 날짜(dateTo) 하루만 보여주도록 의도적으로 그대로 뒀고, 화면 문구에 그 사실을 명시했다.
- **`get_channel_dow_daypart_pattern`/`get_channel_top_programs`(신규 섹션)**: "최근 12주 요일×시간대 강세 히트맵"과 "시청률 상위 콘텐츠 TOP 20". skyUHD처럼 매일 갱신되지 않는 채널도 12주 누적으로 보면 패턴이 보인다는 사용자 설명에 따라, 이 두 섹션은 **기간 선택과 무관하게 항상 dateTo 기준 최근 12주(84일) 고정 윈도우**다(다른 섹션과 다르게 설계한 부분, 의도적).
- **OPPORTUNITY? "최근 1주"를 선택 기간 길이로 일반화**: `get_channel_daypart_opportunity`의 `p_recent_days`(기본 7)를 route.ts에서 `선택 기간 일수`로 계산해 넘긴다(단일 일자면 기존처럼 7일 유지). `p_full_window_days`도 `max(365, recentDays + 84)`로 함께 늘려 baseline이 항상 "최근 구간" 밖에 남도록 했다(이중포함 방지).
- **WHO IS WATCHING?(Affinity)**: 원래도 `get_target_affinity`가 `p_date_from`/`p_date_to`를 받는 함수라 확장 불필요 — 단일 일자는 기존처럼 dateTo 기준 최근 28일 trailing window, 기간 범위는 선택한 기간을 그대로 쓰도록 route.ts에서 분기만 추가했다.
- **WHY?(원인 추적)/기회 탐지(Opportunity Alert)/오늘의 브리핑 요일·시간대 서술(narrativeSignal)/Fit Score**: 전부 원래도 "기준일 하나 + 자체 trailing window(28일/7일/12주)" 설계라 그대로 두고, 기준일만 선택 기간의 마지막 날짜(dateTo)로 넘기도록 route.ts에서 통일했다 — 새로 바꿀 필요가 없었다.
- **검증**: ENA 채널에서 단일 일자(기존 동작 회귀 없음), 최근 1개월(2026-07-20~08-18, 브리핑·시간대 그래프·WHAT HAPPENED 기간요약·WHO IS WATCHING·HOW DEEPLY 전부 기간 값으로 재계산됨 확인), 직접 선택 2026-01-01~08-18(230일 전체, 가장 넓은 범위)까지 브라우저로 직접 확인 — 전부 200 OK, 콘솔 에러 없음. skyUHD처럼 프로그램 단위 데이터가 없는 채널도 "데이터가 없습니다" 메시지로 정상 처리됨을 확인.

## 11. DoD/WoW 프리셋이 기준일을 옮겨버리던 문제 수정 + 프리셋 목록 통합 (2026-08-20, 같은 날 세 번째 사용자 지시)

**사용자가 지적한 버그**: §10에서 만든 "단일 일자" 프리셋 중 DoD("어제")/WoW("1주 전")를 고르면 `dateTo` 자체가 어제/일주일 전으로 옮겨져, 오늘의 브리핑 등이 그 과거 날짜를 마치 "오늘"인 것처럼("오늘 ENA 시청률은...") 서술했다 — 사용자 지시: DoD/WoW는 "어제/전주의 시청률"이 아니라 "전일 대비 오늘"/"전주 대비 오늘"로 분석해야 한다.

- **근본 원인**: `WHAT HAPPENED?` 표(`get_rating_trend_summary`)와 헤더의 "전일 대비" 줄은 원래도 `dateTo` 하나만 있으면 그 날짜 기준 DoD/WoW/MoM/QoQ/YoY/YTD를 전부 동시에 계산해서 보여준다 — 즉 "전일 대비 오늘" 분석은 `dateTo`가 진짜 "오늘"(최신일)이기만 하면 이미 항상 제공되고 있었다. 그런데 DoD/WoW 프리셋이 `dateTo` 자체를 과거로 옮겨버려서, "오늘"이 더 이상 진짜 오늘이 아니게 되고 그 결과 모든 "오늘 ~" 서술이 실제로는 과거를 가리키는 모순이 생겼다.
- **수정**: DoD/WoW를 "기준일을 옮기는 프리셋"으로 다루지 않기로 했다 — 프리셋 목록에서 아예 뺐다(WHAT HAPPENED? 표에 항상 나오는 전일/전주 대비 비교로 충분하기 때문). 대신 프리셋 목록을 **하나로 통합**했다(예전엔 "단일 일자"/"기간 범위" 두 모드로 나뉘어 있었음): 오늘(최신) / **어제**(신규, 오늘 바로 아래 — PD가 어제 하루 자체를 다시 조회하고 싶을 때) / 지난 7일 / 지난 1달 / 연간(1월 1일~오늘) / 직접 선택(from~to, 같은 날짜 두 개를 고르면 자연히 "하루"가 됨). MoM/QoQ도 같은 이유(기준일 이동)로 프리셋에서 뺐다 — WHAT HAPPENED? 표에 전월/전분기 대비가 이미 항상 나온다.
- **"오늘" 오표기 부수 수정**: 프리셋에서 "어제"를 남겨두는 이상, "어제"를 선택했을 때 오늘의 브리핑 등이 여전히 "오늘 ~"라고 서술하면 같은 문제가 재발한다 — `referenceLabel`(선택한 날짜가 최신일과 다르면 `"이 날(YYYY-MM-DD)"`, 같으면 `"오늘"`)을 계산해 브리핑 문단·WHAT HAPPENED? 표의 기준 행·COMPARED WITH? 라벨에 전부 반영했다.
- **검증**: ENA에서 "어제" 선택 시 헤더가 "기준일: 2026-08-17"로, 브리핑이 "이 날(2026-08-17) ENA 시청률은..."으로, WHAT HAPPENED? 표의 기준 행이 "이 날(2026-08-17)"로 표시되고 "전일 대비" 행이 2026-08-16 대비 값을 정확히 보여줌을 브라우저로 확인. "지난 7일" 선택 시에도 정상 동작(2026-08-12~08-18) 확인. 콘솔 에러 없음.

## 12. DoD/WoW/MoM/QoQ/YoY를 "비교 분석" 프리셋으로 재도입 — 달력 기준 이번/전 기간 (2026-08-20, 같은 날 네 번째 사용자 지시)

§11에서 DoD/WoW/MoM/QoQ를 "기준일 이동" 방식이 잘못됐다는 이유로 프리셋 목록에서 뺐는데, 사용자가 다시 명시적인 순서로 되살려 달라고 요청했다 — 단 이번엔 "기준일을 옮기는" 방식이 아니라 "전일 대비 오늘"/"전주 대비 이번주"/"전월 대비 이번달"/"전분기 대비 이번분기"/"전년 동기 대비 이번년도 누적" 형태의 **명시적 비교 분석**으로. 최종 프리셋 순서(사용자가 지정): 오늘(최신) → 어제 → 지난 7일(종합 데이터) → 지난 1달(종합 데이터) → 연간(종합 데이터) → 직접 선택 → 전일 대비 오늘 분석(DoD) → 전주 대비 이번주 분석(WoW) → 전월 대비 이번달 분석(MoM) → 전분기 대비 이번분기 분석(QoQ) → 전년 동기 대비 이번년도 누적 분석(YoY).

- **"지난 7일"류와 "WoW"류의 차이 — 트레일링 윈도우 vs 달력 정렬**: "지난 7일"은 오늘 기준 트레일링 7일(예: 오늘이 수요일이면 지난주 목~이번주 수)이고 "종합 데이터"(그 기간의 평균·시간대 패턴 등 전반적인 지표)를 보여주는 용도다. 반면 "WoW"는 **"이번주"(이번주 월요일~오늘)를 "전주의 같은 요일 구간"(지난주 월요일~지난주의 같은 요일)과 비교**하는 게 목적이라 트레일링 윈도우로는 안 되고, 달력 주 경계(월요일 시작, 이 앱의 기존 월~일 요일 표기와 동일)에 맞춰 "이번 기간"과 "전 기간" 둘 다 계산해야 한다. MoM/QoQ/YoY도 마찬가지로 각각 "이번달 1일~오늘"/"이번분기 시작월 1일~오늘"/"올해 1월1일~오늘"을 "전달/전분기/작년의 같은 상대 위치까지"와 비교한다.
- **`get_rating_period_report`의 기존 "직전 동일 길이 기간" 자동 계산으로는 안 되는 이유**: 이 함수는 원래 "선택 기간 시작일 바로 앞으로 같은 일수만큼"을 전 기간으로 자동 계산한다(§10) — WoW에서 "이번주"가 화요일까지 2일뿐이면 자동 계산은 "그 2일 바로 앞"인 지난주 일~월을 비교 대상으로 삼아버려 "지난주 월~화"와 비교되지 않는다. 그래서 `get_rating_period_report`에 `p_prior_date_from`/`p_prior_date_to`(둘 다 기본 null)를 추가해, 값을 명시적으로 주면 그 날짜를 그대로 쓰고 안 주면(기존 last7/last30/ytd/custom 호출) 기존 자동 계산 그대로 동작하도록 했다(`20260820060000_period_preset_calendar_comparisons.sql`, 인자 추가라 이전에도 겪은 함정대로 `drop function` 선행 필요).
- **"이번 기간"/"전 기간" 계산은 프런트엔드에서**: `ChannelDeepDive.tsx`의 `computeComparisonRange()`가 달력 기준 날짜를 계산해 `dateFrom`/`dateTo`(이번 기간)와 `priorDateFrom`/`priorDateTo`(전 기간) 둘 다 API에 넘긴다. 월/분기/년 단위 이동은 §10의 MoM/QoQ 월말 롤오버 버그에서 썼던 `addMonthsClamped` 패턴을 재사용(대상 월의 마지막 날짜로 클램프)해 "1/31 - 3개월"류 롤오버를 피했다.
- **DoD는 "이번 기간"이 하루뿐인 특수 케이스**: 기존엔 `dateFrom !== dateTo`(며칠짜리 범위)일 때만 "기간 리포트" 스타일(오늘의 브리핑이 문단형 요약, HOW DEEPLY?가 기간 평균)로 렌더링했는데, DoD는 "이번 기간"이 오늘 하루뿐이라 이 조건에 안 걸린다. 그래서 렌더링 모드를 서버가 내려주는 `isRangeMode`(날짜 차이만으로 판단) 대신 프런트엔드에서 `showComparisonView = isComparisonPreset || (dateFrom !== dateTo)`로 재정의해, DoD를 포함한 5개 비교 프리셋은 항상 "기간 리포트" 스타일로 보이게 했다(COMPARED WITH?·02~26시 그래프 라벨·daypart 문구처럼 "그 하루 자체"를 다루는 게 맞는 섹션은 여전히 서버의 `isRangeMode`를 그대로 쓴다 — DoD여도 COMPARED WITH?는 "오늘 시청률" 표기가 맞기 때문).
- **YoY의 알려진 한계**: 2025년은 일별 데이터가 없고 연간 평균(`annual_2025` source_type)만 있어서(DATA_DICTIONARY.md §3), YoY 프리셋의 "전년 동기(2025-01-01~작년 오늘)" 구간을 `get_rating_summary`로 조회하면 0건이 나와 `prior_period_change_pct`가 NULL이 된다 — 화면은 이 경우 비교 줄 자체를 표시하지 않는다(NULL을 0으로 바꿔치기하지 않음, CLAUDE.md 원칙). 실데이터로 확인된 정상 동작이며 버그 아님.
- **검증**: ENA에서 DoD(오늘 vs 전일, WHAT HAPPENED? 표의 DoD 행과 동일한 ▲228.9% 확인) / WoW(2026-08-17~19 vs 2026-08-10~12, ▲7.3%, 직접 SQL 테스트와 동일값) / MoM(2026-08-01~19 vs 2026-07-01~19, ▼13.8%) / QoQ(2026-07-01~19 vs 2026-04-01~05-19, ▼16.6%) / YoY(2026-01-01~19, 비교 줄 없음 — 2025 일별 데이터 없어 정상) 전부 브라우저로 확인, 프리셋 순서도 사용자가 지정한 순서 그대로 렌더링됨을 확인. 콘솔 에러 없음(HMR 프록시 웹소켓 아티팩트 제외).

## 13. 주간/월간/연간 집계 파일을 "검수 참고 자료"로 활용 — `scripts/verify-against-nielsen-aggregate.mjs` (2026-08-20)

사용자 지시: 8/10~8/16 주간 집계 파일처럼 "정확한 주간 순위·시청률"이 이미 계산돼 들어있는 닐슨 주간/월간/연간 파일을, 우리가 일별 파일을 쌓아 SQL로 직접 계산한 같은 기간 평균과 대조해 "우리 계산이 맞는지 검수"하는 데 쓰라는 지시. 정식 업로드 기능(주간/월간 파일 거부)은 그대로 두고, **검수 전용 스크립트**를 새로 만들었다(DB 스키마·UI 변경 없음 — CLAUDE.md "필요할 때마다 확장" 원칙, 이건 상시 반영이 아니라 필요할 때 실행하는 도구라 영구 테이블을 만들지 않았다).

- **파싱 로직은 새로 만들지 않았다**: 실제 파일을 열어 확인해보니 주간(`260810-260816`)/월간/연간 파일 모두 이미 §3에 문서화된 연간 파일과 **완전히 같은 구조**(`유료방송가입가구`/`개인` 2개 시트, 다단 헤더, 타깃 블록이 가로로 반복)다 — 날짜 범위만 다를 뿐 시트 포맷은 동일해서, `nielsenDaily.ts`의 `parseRankSheet` 알고리즘을 그대로 복제해 썼다(TS 모듈이라 순수 Node 스크립트에서 직접 import가 안 돼, `scripts/backfill-skyuhd-channel-rank.mjs`가 이미 쓴 방식대로 복제 — 결과가 100% 같음을 확인).
- **동작**: 파일명(`YYMMDD-YYMMDD`)에서 날짜 범위를 뽑고, 랭킹 시트에서 우리 7개 채널 × 각 타깃 블록의 "닐슨이 계산한 그 기간 평균 시청률"을 읽은 뒤, 같은 채널·타깃·기간으로 `get_rating_summary()`(§1에서 이미 쓰는 함수, 새 계산 로직 없음)를 호출해 우리 쪽 평균과 비교한다. 오차 허용은 절대 0.001 또는 상대 1% 중 큰 쪽(반올림 차이 흡수용).
- **검증 결과**: 2026-08-10~08-16 주간 파일로 실행한 결과 **56개 채널×타깃 조합 전부 일치**(소수 5자리까지 반올림 오차 수준). 우리 SQL 계산이 닐슨 공식 집계와 정확히 같다는 것을 실제로 확인했다.
- **알려진 한계**: 여러 파일을 한 번에 훑을 때(`node scripts/verify-against-nielsen-aggregate.mjs Nielsen Data/2026`) 요청이 짧은 시간에 몰리면 일시적 "fetch failed"가 실제로 발생해, 요청 사이 30ms 지연 + 실패 시 1회 재시도를 추가했다. 2025년 파일은 일별 데이터가 없어(§3, `annual_2025` 연간 평균만 있음) "데이터없음"으로 나오는 게 정상이다.
- **사용법**: `node --env-file=.env scripts/verify-against-nielsen-aggregate.mjs [파일 또는 폴더 경로]` (인자 없으면 `Nielsen Data/` 전체를 훑음).

## 14. Page 1 ENA 히어로 카드 재구성 + 로고 교체 + 시청률 표시 자릿수 통일 (2026-08-20)

사용자 지시(같은 날 다섯 번째): ① 채널 로고를 `채널 로고/` 폴더의 새 파일로 교체, ② 시청률 표시는 전부 소수점 3자리로 통일(계산은 원본 정밀도 그대로), ③ ENA 히어로 카드를 로고·시청률 가운데 정렬 + 시청률(순위)·전일 대비 + 올해 누적 평균 시청률(순위)·목표 순위 대비 격차로 재구성, ④ 하단 6채널(ENA Play/ENA Drama/ENA Story/OLIFE/ONCE/skyUHD, 이 순서)은 시청률·순위·전일 대비가 한 줄에.

- **로고 교체**: `public/channel-logos/{CODE}.png`를 `채널 로고/` 폴더의 새 파일로 덮어썼다(`scripts/backfill-logo-visible-ratio.mjs`로 `logo_visible_ratio`/`logo_visible_top_ratio` 재계산 — 새 로고는 대부분 투명 여백이 거의 없어짐, ENA/OLIFE/skyUHD/ENA Play/ENA Drama/ENA Story 전부 `visibleRatio≈1.0`으로 바뀜). "kt ENA CI (검/흰).png" 2개는 채널 로고가 아니라 별도 CI 마크로 보여 이번 교체에는 쓰지 않았다(용도 불명확 — 필요하면 알려달라).
- **시청률 자릿수**: `Dashboard.tsx`의 `formatRating`과 `ChannelDeepDive.tsx`의 `fmt` 기본 자릿수를 4~5자리에서 3자리로 낮췄다. DB·SQL 계산은 원본 정밀도(5자리) 그대로 유지되고 화면 표시만 반올림된다.
- **`get_channel_period_rank_and_rating`(신규)**: ENA 히어로의 "누적(1/1~오늘) 평균 시청률·순위"용. **경쟁채널 시청률을 다시 모아 순위를 재계산하지 않는다** — Nielsen 랭킹 시트가 이미 매일 전체 시장 기준 순위를 계산해 `ratings.rank`에 넣어주므로 그 값의 기간 평균만 낸다. Page 1 API가 "오늘 순위" 조회 시 이미 라벨 표기 불일치(§1.1)를 candidate 라벨 재시도로 풀어 찾아둔 `target_id`를 그대로 재사용해, 라벨 재해석 로직을 중복하지 않았다.
- **목표 순위 대비 격차**: `target_goals.target_rank`(이미 있는 컬럼, `get_target_achievement`가 이미 반환)를 그대로 읽는다 — ENA는 "6"(숫자 텍스트), skyUHD처럼 "경쟁채널 중 2위" 같은 비숫자 값은 `parseInt`가 실패해 격차 문구를 생략한다(단정하지 않음).
- **검증**: 브라우저로 확인 — ENA 히어로가 "0.184(4위)" · "▲ 228.9%" · "누적(1/1~오늘) 평균 0.120(9.3위) · 목표 순위(6위) 대비 3.3위 낮음"으로 정상 렌더링, 하단 6채널이 요청된 순서(ENA Play→ENA Drama→ENA Story→OLIFE→ONCE→skyUHD) 그대로 한 줄 레이아웃으로 표시됨을 확인. 새 로고 7개 전부 200 OK로 로드됨. 콘솔 에러 없음.
- **미해결**: "1페이지 상단은 첫화면 예제 파일을 참고" — 사용자가 언급한 참고 이미지 파일을 프로젝트 폴더에서 찾지 못했다(첨부되지 않음). 텍스트로 준 상세 스펙(가운데 정렬, 시청률(순위)+전일대비, 누적 평균 줄)만으로 히어로 카드를 재구성했고, 이미지를 공유해주면 세부 톤앤매너를 추가로 맞출 수 있다.
- **후속(같은 날 mid-turn 지시)**: 관리자 화면 이동 버튼을 "⚙ 관리자 화면" 텍스트 버튼에서 "⚙" 아이콘 전용 원형 버튼으로 축소. 좌측 최상단 로고를 ENA 채널 로고(동적) 대신 고정 "KT ENA CI(검)" 브랜드 마크로 교체(`public/kt-ena-ci-black.png` — 사용자가 준 `E:\★ 시청률 AI agent 개발용 파일\채널 로고\` 경로 대신, my-app 프로젝트의 `채널 로고/` 폴더에 이미 동일 파일(파일 크기·수정일 동일)이 있어 그걸 사용, CLAUDE.md "새 파일은 my-app 안에만" 원칙).

---

## 15. Page 1 Original 리포트 심화(재방·경쟁 비교·헤드라인) + Page 2 채널명 색상 + 기간 설정 최종 조정 (2026-08-20)

사용자가 첨부한 "나는 SOLO" 265/266회 참고 리포트(닐슨 원본 파일이 아니라 PD가 수기로 만드는 훨씬 상세한 내부 리포트 — 분당 시청률·플랫폼별(IPTV/CATV/SKYLIFE)·PD 코멘트까지 포함)를 열어 톤을 참고했다. 이 리포트 수준의 전체 재현은 우리 데이터(닐슨 일별 파일)의 정밀도를 넘어서 하지 않고, 사용자가 명시적으로 요청한 항목만 실제 데이터로 구현했다.

- **ENA Play 직후재방이 지금까지 전혀 안 잡히던 원인**: 코드 버그가 아니라 `original_review_programs.rerun_channel_id`가 "나는 SOLO" 두 행 모두 NULL이었다(관리자가 아직 안 채운 값) — ENA_PLAY로 채워 넣었다. 추가로 "본방 종료시각 이후 첫 편성"이라는 기존 조건이 실데이터로 깨지는 걸 발견했다: ENA Play의 재방(00:13:58 시작)이 ENA 본방 종료시각(00:14:20)보다 22초 **먼저** 시작하는 것으로 기록돼 있어(두 채널이 같은 순간을 초 단위로 다르게 기록) `start_time > end_time` 조건에 걸려 다음 프로그램이 잘못 뽑히고 있었다 — 기준을 "본방 종료"가 아니라 "본방 **시작**시각 이후 첫 편성"으로 바꾸고, 자정을 넘는 시각 비교라 02시 이전은 +24시간 보정한 day-boundary-safe 비교로 고쳤다(실데이터로 직접 재현·확인).
- **더 심각한 기존 버그(검증 중 발견, 요청과 별개로 같이 고침)**: "OOO타깃상세" 시트는 같은 프로그램 블록에 P2049/여20대/남20대 등 17개 타깃 행이 나란히 있는데, 재방/선행재방/당일재방/직전방영 조회 전부가 target_id를 전혀 걸지 않고 있었다 — Postgres가 우연히 먼저 돌려주는 아무 타깃의 시청률을 가져오고 있었다(채널의 KPI 타깃과 무관하게 뒤섞일 수 있었음). `channels.primary_target`을 타깃상세 표기로 변환하는 규칙(`resolveProgramLevelTargetLabel`과 동일, SQL로 복제)을 모든 조회에 일관되게 걸어 고쳤다.
- **ENA 자체 선행재방(전주 회차)·당일 자체재방**: 22:30 본방 전 20:36에 전주 회차를, 본방 종료 후 00:33(=24:33)에 당일 자체 재방을 방영하는 패턴을 실데이터로 확인 — `get_original_content_daily`에 `pre_rerun_*`/`self_rerun_*` 컬럼을 추가해 리드인 배율·유지율까지 계산해 문단으로 조립한다(`buildRerunInsight`, Dashboard.tsx).
- **SBS Plus는 ENA의 등록 경쟁채널이 아니라 ENA Drama의 등록 경쟁채널**(§1.2 고정 페어링) — ENA "나는 SOLO"와 SBS Plus 동시방송(22:29:58, 라이브 심의트)을 비교하려면 ENA Drama 쪽 `competitor_program_ratings`를 봐야 한다. `get_competitor_overlap_via_channel(조회할_채널, 경쟁채널명, ...)` 범용 함수를 새로 만들어(이름 하드코딩 없이 설정 배열로 관리) 해결했고, 여기서도 같은 day-boundary 비교 버그(22:29를 00:14와 raw 비교하면 "더 이르다"로 잘못 판정)를 발견해 함께 고쳤다.
- **회차 번호**: 닐슨 자료엔 회차 정보가 전혀 없다(프로그램명이 "나는SOLO"로만 들어옴) — `program_episode_counters` 테이블에 관리자가 한 번 심어둔 기준점("나는SOLO" 267회=2026-08-19, 사용자가 알려준 사실)부터 "그 프로그램이 실제로 방영된 날짜 수"를 세어 앞으로 자동으로 이어간다(`get_episode_number`). 결방/특집으로 인한 오차를 피하려 seed 이전 날짜는 계산하지 않는다(추정 대신 NULL).
- **헤드라인**: "<프로그램> N회 본방송 시청률 (전회 대비 상승/하락, 동시간대 타깃 1위)" 형식(`buildOriginalHeadline`, Dashboard.tsx) — 회차 번호가 있는 프로그램에만 표시. "전회 대비"는 같은 화이트리스트 프로그램·채널·타깃의 가장 최근 이전 방영(`prior_occurrence_*`, 최대 21일 전까지 탐색)과 비교, "동시간대 타깃 1위"는 이미 계산된 competitorHighlights 중 최고 시청률과 비교해 판정.
- **Page 1 채널별 인사이트 — 채널명 색상+BOLD**(사용자 지시): `buildChannelNarrative`/`buildSkyUhdNarrative`가 문자열 대신 `{channelName, text}`를 돌려주도록 바꾸고, `channels.theme_color`로 채널명만 굵게 색칠해서 렌더링.
- **Page 2 좌측 상단 채널 로고**: 이미 `ChannelSidebar.tsx`가 `next/image`로 로고를 렌더링하고 있었다(기존 구현, 이번에 새로 만든 게 아님) — 브라우저 네트워크 요청으로 7개 전부 200 OK 확인, 별도 수정 불필요했음.
- **Page 2 기간 설정 순서 재조정(같은 날 마지막 지시가 최종)**: "직접 선택"을 "오늘" 바로 다음으로 이동, "DoD"를 "어제 대비 오늘 분석"으로 라벨 변경, WoW를 §12의 "달력 주(월요일 시작)" 정의에서 **"오늘 포함 지난 7일 vs 지난 8~14일차"(트레일링)**로 재정의 — 최종 순서: 오늘→직접선택→어제→지난 7일→지난 1달→연간→어제 대비 오늘 분석→WoW→MoM→QoQ→YoY.
- **오늘의 브리핑 심화(사용자 지시)**: 기간 범위/비교 분석 프리셋 선택 시 시청률뿐 아니라 **점유율·시청시간·연령대·주요 프로그램**까지 종합해 "무엇이 상승/하락을 이끌었는지" 서술하도록 확장. `get_rating_period_report`에 전 기간의 점유율·도달율·시청시간 추가, `get_channel_period_demographics`(연령대별 이번/전 기간 비교)·`get_channel_period_program_movers`(프로그램별 이번/전 기간 비교, |등락폭| 큰 순) 신규 — 프로그램 드라이버는 "가장 크게 움직인 프로그램"을 짚어줄 뿐 인과관계로 단정하지 않는다(CLAUDE.md 원칙).
- **검증**: 8/19 ENA "나는 SOLO"로 헤드라인("<나는SOLO> 267회 본방송 시청률 (전회 대비 상승, 동시간대 타깃 1위)"), 선행재방(20:36·0.222)·당일재방(00:33·0.355)·ENA Play 직후재방(00:13·0.016)·SBS Plus(22:29·0.752) 전부 실데이터로 정상 표시 확인. Page 2 WoW(2026-08-13~19 vs 08-06~12, ▲3.5%)로 브리핑에 연령대(여20대 ▲165.4%)·신규 프로그램 기여("신병3몰아보기") 문장이 실제로 붙는 것을 확인. 콘솔 에러 없음(HMR 관련 잔여 로그는 새 탭에서 재확인해 없음을 교차 검증).

---

## 16. Page 1/2 마무리 보강 — TOP20 점유율 코멘트·12주 기준선·skyUHD 프로그램 분석 버그·소수점 자릿수 예외 등 (2026-08-20)

같은 날 마지막 사용자 지시(다수 항목)를 반영하며 발견·수정한 것들.

- **skyUHD "프로그램 분석/시간대별 분석이 안 나온다" 버그 — 원인 2가지, 둘 다 수정**: (1) skyUHD 프로그램 단위 `ratings`는 타깃 구분이 없는 원본 특성상 `target_id`가 전부 NULL인데(CLAUDE.md 설계상 의도), Page 2의 프로그램 단위 쿼리가 전부 `join targets t on ... where t.label = ...`(INNER JOIN)이라 NULL 행이 통째로 걸러지고 있었다 — `left join targets ... and (t.label = p_program_target_label or r.target_id is null)`로 6개 함수(`get_hourly_rating_pattern`/`get_hourly_program_titles`/`get_channel_dow_daypart_pattern`/`get_channel_top_programs`/`get_channel_daypart_opportunity`/`get_channel_daily_narrative`) 전부 수정(`20260820130000_skyuhd_null_target_tolerance.sql`). (2) 그런데도 여전히 0건이길래 재조사한 결과, skyUHD 프로그램 단위 행은 `source_type = 'skyuhd'`(수기 업로드 전용 값)인데 6개 함수 모두 `r.source_type = 'nielsen_daily'`로 하드코딩돼 있었다(진짜 주 원인) — `r.source_type in ('nielsen_daily', 'skyuhd')`로 수정(`20260820140000_skyuhd_source_type_fix.sql`, 채널 단위 부분은 의도적으로 `nielsen_daily`만 유지 — skyUHD의 채널 단위 순위는 §1.1 랭킹 시트에서 나오므로). 직접 SQL 재현 테스트로 두 원인 모두 실증(1번만 고쳤을 때도 0건 → 2번까지 고치니 정상 데이터 확인).
- **TOP20 콘텐츠 — "시청률은 약해도 점유율은 상대적으로 좋은" 프로그램 코멘트**: `get_channel_top_programs`가 이미 `avg_share`를 반환하므로 새 SQL 없이, 프론트(`findShareOutliers`)에서 TOP20 목록 내 시청률 순위와 점유율 순위를 비교해 점유율 순위가 5순위 이상 앞서면 인라인 코멘트를 붙인다(새 계산 없음, 이미 있는 값의 순위만 비교).
- **채널별 킬러 콘텐츠(Page 1) — 점유율·유료가구 코멘트**: `get_channel_killer_content_daypart`에 `avg_share`/`channel_avg_share_baseline`(같은 창의 채널 전체 프로그램 평균 점유율)/`household_avg_rating`/`household_baseline_avg_rating`(전국 유료가구, KPI가 이미 유료가구인 채널은 `p_program_target_label <> '전국 유료가구'` 조건으로 NULL 처리)을 추가(`20260820150000_killer_content_share_household.sql`). 프론트에서 ±15% 이상 차이 나면 코멘트 표시.
- **02~26시 그래프 — 최근 12주 시간대별 평균 시청률 기준선(연한 꺾은선)**: 새 SQL 불필요 — 기존 `get_hourly_rating_pattern`을 dateTo 기준 직전 84일 창으로 한 번 더 호출해(`hourlyBaselinePattern`) 프론트에서 막대와 같은 좌표계의 SVG `<polyline>`으로 오버레이했다. 막대 높이 계산에 쓰는 `maxByMetric.avg_rating`과 같은 스케일을 써서 시각적으로 비교 가능하게 했고, 라벨 행(시간 숫자)과 막대 영역을 별도 컨테이너로 분리해야 SVG viewBox가 막대 높이와 정확히 겹친다는 걸 확인했다(라벨 텍스트 높이가 섞이면 좌표가 어긋남).
- **skyUHD 소수점 자릿수 예외(1페이지 4자리, 2페이지 5자리, 나머지 전 채널 3자리 고정, 사용자 지시)**: `Dashboard.tsx`의 `formatRating(v, channelCode)`는 `channelCode === "SKYUHD"`일 때만 4자리. `ChannelDeepDive.tsx`는 컴포넌트 전체가 이미 "채널 하나만 보여주는" 구조라 `code === "SKYUHD"`를 파라미터로 스레딩해야 했는데, 처음에는 모듈 스코프 변수(`let CURRENT_PAGE_IS_SKYUHD`)를 컴포넌트 렌더 시작 시 대입하는 방식으로 구현했다가 **ESLint(`react-hooks/globals`, "렌더 중 컴포넌트 밖 변수를 재대입하면 안 된다")에 바로 걸렸다** — React 규칙을 어기는 실수였음을 인지하고, `data: ChannelData`를 받는 줄글 조립 함수는 함수 내부에서 `data.channel.code`로 지역 `fmtR`을 만들고, `data`를 받지 않는 함수(`buildHowDeeplyExplanation`/`buildOpportunityNarrative`)는 `isSkyUhd: boolean` 파라미터를 추가해 호출부(`code === "SKYUHD"`)에서 넘기는 방식으로 다시 고쳤다. 컴포넌트 렌더 본문(JSX)에서 직접 쓰는 `fmt(`는 지역 `fmtR = (v) => fmt(v, code === "SKYUHD" ? 5 : 3)`로 일괄 치환.
- **Page 1 채널별 인사이트 — "평균 대비 엄청난 하락을 이끈 프로그램" 코멘트**: `get_channel_daily_narrative`에 `decline_program_*`(오늘 방영 프로그램 중 자기 자신의 최근 12주 평균 대비 가장 크게, -30% 이상 하락한 프로그램) 추가. **처음 배포 후 실데이터로 확인하니 거의 매일 모든 채널에서 새벽 필러 프로그램(예: 최근 평균 0.009~0.026 수준)이 오늘 0.000을 기록한 것만 잡히는 노이즈였다** — 새벽 저시청률 슬롯은 표본이 작아 0.000이 매우 흔해서 통계적으로 "-100% 하락"이 거의 항상 발생하는 것이지 코멘트할 가치가 있는 진짜 "엄청난 하락"이 아니었다. `baseline_avg >= 0.05`(웬만한 낮 시간대 프로그램 수준) 최소 규모 조건을 추가해 걸러냈다(`20260820170000_decline_program_noise_floor.sql`) — 수정 후에는 '유퀴즈온더블럭'(74% 하락)·'극한직업'(62% 하락)류의 실제로 의미 있는 하락만 잡히는 것을 확인.
- **최근 12주 요일×시간대 히트맵 전치(사용자 지시)**: 기존엔 요일이 행, daypart가 열이었는데 반대로(daypart가 행, 요일이 열) 바꿔달라는 요청 — SQL 변경 없이 프론트 테이블 렌더링 순서만 전치.
- **OPPORTUNITY? 워딩**: "▼ 약화" → "▼ 약세"(사용자 지시, 단순 텍스트 수정).
- **날짜 옆 요일 표기**: `src/lib/dateFormat.ts`에 `formatDateWithDow()` 신규(연/월/일로 쪼개 로컬 `Date`를 만들어 `getDay()`로 요일 계산 — `toISOString()` 타임존 버그를 피하기 위해 §11에서 이미 확립한 패턴 재사용). Page 1 헤더, Page 2 "기준일"/"기간"에 적용.
- **Original 리포트 "오리지널 예능" 태그**: `featured_content`는 Channel Master "KT ENA 오리지널" 시트에서 **편성 정보(요일·시간)가 기입된 항목만** 반영하는 기존 설계(의도적, 이전 세션에서 확정) 때문에 "나는 SOLO"/"나는 SOLO, 그 후 사랑은 계속된다"는 편성 정보가 없어 빠져 있었다 — 이번엔 사용자가 명시적으로 "오리지널 예능 태그를 붙여달라"고 지시해 두 프로그램만 `featured_content`에 수동으로 추가했다(`category='오리지널 예능'`). **부수적으로 발견한 사실**: `programs` 테이블에 같은 `canonical_name`("나는SOLO", "나는SOLO그후사랑은계속된다")을 가진 행이 각각 2개씩 존재한다(원인 미조사) — `featured_content`↔`programs` 매칭은 ID가 아니라 `replace(canonical_name, ' ', '')` 문자열 비교라 어느 쪽 ID를 골라도 화면 표시에는 영향이 없어, 임의로 하나씩 골라 삽입했다.
- **수요일 화이트리스트 정리**: "요일 별 리뷰 프로그램" 시트에는 여전히 수요일에 "아이돌 파견근무"가 함께 적혀 있지만(원본 파일 그대로), 사용자가 "수요일은 나는 SOLO만 분석하면 돼"라고 명시적으로 지시해 `original_review_programs`에서 그 행만 직접 삭제했다 — **원본 엑셀 파일과 DB 상태가 어긋난 상태**이므로, 다음에 관리자가 채널기본정보.xlsx를 재업로드하면(매번 전체 교체 방식) 이 행이 되살아난다는 점을 사용자에게 알려야 한다.

---

## Version History

| Version | Date | Changes |
|---|---|---|
| 0.1 | 2026-08-19 | 2025~2026년 Nielsen 일별/연간 파일 10개 시트 + skyUHD 파일 구조를 실제 샘플로 확인해 최초 작성. 2013~2016년 과거 자료는 보류 처리. |
| 0.2 | 2026-08-19 | 개발 단위 16번(Affinity·Competitive Pressure) 구현 중 발견한 사실 추가: `competitor_ratings`는 §1.1 랭킹 시트에서만 나오며 라벨이 8종류로 제한됨, 자사 매칭 라벨과의 3-way 불일치(ENA 계열), Postgres `LEAST(100,NULL)` 함정, Affinity의 수도권/전국 스코프 불일치. |
| 0.3 | 2026-08-19 | 개발 단위 17번(Fit Score MART) 설계 판단 추가: Program 단위 정의, Daypart 4구간 임의 정의, Target Affinity·Competitive Opportunity의 채널 단위 근사, percentile 결측시 50 중립값 처리, Confidence 계산식, SQL 버그 3건(round 캐스팅, GROUP BY 별칭, 계산 순서 의존성). |
| 0.4 | 2026-08-19 | 개발 단위 19번(원인 추적·기회 탐지) 설계 판단 추가: "편성 변화" 감지를 프로그램명 단위가 아니라 경쟁채널 채널 단위 시청률 변동으로 축소한 이유와 근거, 기준 기간 선택, 실데이터 트리거 검증 결과. |
| 0.5 | 2026-08-19 | §1.2 `OOO경쟁채널시청률` 시트 우측 블록의 채널 페어링을 실데이터로 재확인(ENA↔tvN, ENA DRAMA↔ENA STORY, ENA PLAY↔MBC every1)하고 새로 저장하기 시작(`competitor_program_ratings`). 단, R4 헤더 채널명과 실제 프로그램명이 다른 경우가 다수 발견돼(예: tvN 헤더인데 MBC뉴스데스크 등장) 정확한 산출 기준을 PD팀 확인 전까지 화면 노출은 보류. |
| 0.6 | 2026-08-19 | §1.2를 실데이터로 재조사해 "우측 블록 1개=페어링된 경쟁채널 1개"가 아니라 "5행×2열 그리드=경쟁채널 8~9개 전체"임을 확정, 예전 파서가 블록 경계를 무시하고 시트 끝까지 읽던 버그였음을 확인·수정(0.5의 미해결 의문 해소, 화면 노출 보류 해제). 230일 전체 재백필(147,458건). §8(Original 리포트 화이트리스트) 신규 작성 — 요일별 프로그램 화이트리스트 매칭 방식과 "이름 먼저 일치 후 ±10분 매칭"으로 고친 버그를 문서화. |
| 0.7 | 2026-08-20 | §9(Page 2 재설계) 신규 작성 — 보고서형 화면으로 전환하며 추가한 함수(`get_competitor_insight_report`/`get_channel_daypart_opportunity`/`get_hourly_program_titles`)와 기간 설정 UI, 그리고 이번 회고 리뷰에서 고친 4건(daypart 격차 계산의 최근 구간 이중포함, MoM/QoQ 월말 롤오버, Fit Score API 동시요청 경합, anon 역할 statement_timeout 3초 제한 — 브라우저로 직접 재현해 근본 원인 확인) 기록. |
| 0.8 | 2026-08-20 | §10 신규 작성 — 기간 설정을 단일 일자에서 기간 범위(YYYY-MM-DD~YYYY-MM-DD)로 확장(사용자 지시): `get_rating_period_report` 신규, `get_hourly_program_titles`/`get_competitor_insight_report`/`get_competitor_top_programs` 범위 확장(파라미터 추가 시 Postgres가 오버로드로 취급해 `drop function` 먼저 필요했던 함정 포함), daypart 기회 분석의 "최근 1주"를 선택 기간 길이로 일반화, 신규 섹션(요일×시간대 히트맵/TOP 20 콘텐츠)은 기간 선택과 무관하게 항상 최근 12주 고정. |
| 0.9 | 2026-08-20 | §11 신규 작성 — DoD/WoW 프리셋이 기준일 자체를 과거로 옮겨 "오늘의 브리핑"이 어제/전주를 "오늘"인 것처럼 서술하던 버그 수정(사용자 지시): DoD/WoW/MoM/QoQ를 기준일-이동 프리셋에서 제거(WHAT HAPPENED? 표가 이미 항상 전일/전주/전월/전분기 대비를 보여주므로 불필요), "단일 일자"/"기간 범위" 두 모드를 프리셋 하나로 통합(오늘/어제/지난 7일/지난 1달/연간/직접 선택), "어제" 등 비-최신일 조회 시 "오늘" 대신 "이 날(YYYY-MM-DD)"로 서술하도록 `referenceLabel` 도입. |
| 1.0 | 2026-08-20 | §12 신규 작성 — DoD/WoW/MoM/QoQ/YoY를 "기준일 이동" 대신 "달력 기준 이번 기간 vs 전 기간" 명시적 비교 분석 프리셋으로 재도입(사용자 지시, 지정한 순서 그대로): `get_rating_period_report`에 `p_prior_date_from`/`p_prior_date_to` 추가, 프런트엔드가 달력 정렬로 이번/전 기간을 계산(`computeComparisonRange`), DoD(하루짜리 비교)를 위해 렌더링 모드 판단을 서버 `isRangeMode`에서 클라이언트 `showComparisonView`로 분리, YoY의 2025 일별 데이터 부재로 인한 NULL 처리(정상) 기록. |
| 1.1 | 2026-08-20 | §13 신규 작성 — 닐슨 주간/월간/연간 집계 파일을 우리 SQL 계산 검수용으로 쓰는 `scripts/verify-against-nielsen-aggregate.mjs` 신규(사용자 지시). 파싱은 기존 `parseRankSheet` 알고리즘 복제(주간/월간도 연간 파일과 동일 구조임을 실제 파일로 확인), 2026-08-10~16 주간 파일로 56개 채널×타깃 조합 전부 일치 확인. |
| 1.2 | 2026-08-20 | §14 신규 작성 — Page 1 ENA 히어로 카드 재구성(가운데 정렬, 시청률(순위)+전일대비+올해 누적 평균 시청률(순위) 대 목표 순위), 채널 로고 7개 전량 교체, 시청률 표시 자릿수를 3자리로 통일(사용자 지시). `get_channel_period_rank_and_rating` 신규 — 경쟁채널 재계산 없이 Nielsen이 이미 계산한 rank 컬럼의 기간 평균만 사용. |
| 1.3 | 2026-08-20 | §15 신규 작성 — Page 1 Original 리포트 심화(사용자 지시): ENA Play 직후재방 인식 버그(rerun_channel_id 미설정 + day-boundary 비교 오류) 수정, 검증 중 발견한 target_id 미필터링 버그(더 심각, 별도) 수정, ENA 자체 선행재방·당일재방 감지, SBS Plus 교차채널 경쟁 비교(`get_competitor_overlap_via_channel`), 회차 번호 추적(`program_episode_counters`+`get_episode_number`, "나는SOLO" 267회=8/19 seed), 헤드라인 생성. Page 1 채널명 색상+BOLD. Page 2 기간 설정 최종 순서 조정 + WoW를 트레일링 7일 비교로 재정의, 오늘의 브리핑에 점유율·시청시간·연령대·프로그램 드라이버 분석 추가(`get_channel_period_demographics`/`get_channel_period_program_movers` 신규). |
| 1.4 | 2026-08-20 | §16 신규 작성 — skyUHD 프로그램 분석 버그(target_id NULL 미허용 + source_type 하드코딩, 2가지 원인 모두 실증) 수정, TOP20/킬러 콘텐츠 점유율·유료가구 코멘트, 02~26시 그래프 12주 기준선 오버레이, skyUHD 소수점 자릿수 예외(ESLint `react-hooks/globals`에 걸려 모듈 스코프 변수 대신 파라미터 스레딩으로 재구현), decline_program 신규(노이즈 플로어 0.05 추가로 새벽 필러 오탐 제거), 요일×시간대 히트맵 전치, "약화"→"약세", 날짜 요일 표기(`formatDateWithDow`), 나는 SOLO "오리지널 예능" 태그 수동 추가(programs 테이블 canonical_name 중복 발견), 수요일 화이트리스트 정리(원본 엑셀과 DB 상태 어긋남 — 재업로드 시 되살아남 주의). |
